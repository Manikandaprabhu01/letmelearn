// Imported from the Interview Prep Console (lib/data-coding-b.js).
import type { CodingAnswer } from "../types";

export const codingB: CodingAnswer[] = [
  {
    id: "treeviews",
    t: "Binary tree: max depth, left view, top view",
    lc: 104,
    slug: "maximum-depth-of-binary-tree",
    d: "M",
    r: 1,
    src: ["S4"],
    stmt: "Three questions in one sitting: return the maximum depth of a binary tree; print the left view (first node of each level); print the top view (first node seen at each horizontal distance).",
    key: "They escalate: depth (easy, recursive), left view (BFS or DFS with depth tracking), top view (BFS with horizontal distance — a DFS here is a trap because it can reach a column via a deeper path first). Say why top view needs BFS and you have answered the real question.",
    brute: {
      i: "Depth: recursion. Left view via DFS is fine. Top view attempted with DFS: record the first node per horizontal distance in preorder — this is subtly wrong, because a node that is deeper but reached earlier in preorder wins over a shallower node in the same column.",
      tc: "O(n)",
      sc: "O(h)",
      java: "int maxDepth(TreeNode root) {\n    return root == null ? 0 : 1 + Math.max(maxDepth(root.left), maxDepth(root.right));\n}\n\n// WRONG top view: DFS order does not equal vertical order\nvoid topViewBuggy(TreeNode n, int hd, Map<Integer, Integer> m) {\n    if (n == null) return;\n    m.putIfAbsent(hd, n.val);           // a deeper node can claim the column first\n    topViewBuggy(n.left, hd - 1, m);\n    topViewBuggy(n.right, hd + 1, m);\n}",
      py: "def max_depth(root):\n    return 0 if not root else 1 + max(max_depth(root.left), max_depth(root.right))\n\n# WRONG top view: preorder is not top-down per column\ndef top_view_buggy(n, hd, m):\n    if not n: return\n    m.setdefault(hd, n.val)\n    top_view_buggy(n.left, hd - 1, m)\n    top_view_buggy(n.right, hd + 1, m)",
    },
    opt: {
      i: "Left view: BFS and take the first node of each level (or DFS keeping maxDepthSeen and recursing left first). Top view: BFS with (node, horizontalDistance); the first time a horizontal distance appears, that node is visible. BFS guarantees the shallowest node per column. Use a TreeMap so the columns come out left-to-right.",
      tc: "O(n) for depth and left view, O(n log n) for top view because of the TreeMap",
      sc: "O(n)",
      java: "List<Integer> leftView(TreeNode root) {\n    List<Integer> out = new ArrayList<>();\n    if (root == null) return out;\n    Deque<TreeNode> q = new ArrayDeque<>();\n    q.add(root);\n    while (!q.isEmpty()) {\n        int size = q.size();\n        for (int i = 0; i < size; i++) {\n            TreeNode n = q.poll();\n            if (i == 0) out.add(n.val);              // first node of the level\n            if (n.left != null) q.add(n.left);\n            if (n.right != null) q.add(n.right);\n        }\n    }\n    return out;\n}\n\nList<Integer> topView(TreeNode root) {\n    List<Integer> out = new ArrayList<>();\n    if (root == null) return out;\n    Map<Integer, Integer> firstAtColumn = new TreeMap<>();\n    Deque<Object[]> q = new ArrayDeque<>();\n    q.add(new Object[]{root, 0});\n    while (!q.isEmpty()) {\n        Object[] cur = q.poll();\n        TreeNode n = (TreeNode) cur[0];\n        int hd = (int) cur[1];\n        firstAtColumn.putIfAbsent(hd, n.val);        // BFS => shallowest wins\n        if (n.left != null)  q.add(new Object[]{n.left, hd - 1});\n        if (n.right != null) q.add(new Object[]{n.right, hd + 1});\n    }\n    out.addAll(firstAtColumn.values());\n    return out;\n}",
      py: "from collections import deque\n\ndef left_view(root):\n    out = []\n    if not root: return out\n    q = deque([root])\n    while q:\n        for i in range(len(q)):\n            n = q.popleft()\n            if i == 0:\n                out.append(n.val)\n            if n.left:  q.append(n.left)\n            if n.right: q.append(n.right)\n    return out\n\ndef top_view(root):\n    if not root: return []\n    first = {}\n    q = deque([(root, 0)])\n    while q:\n        n, hd = q.popleft()\n        first.setdefault(hd, n.val)\n        if n.left:  q.append((n.left, hd - 1))\n        if n.right: q.append((n.right, hd + 1))\n    return [first[k] for k in sorted(first)]",
    },
    fu: [
      {
        q: "Bottom view?",
        a: "Same BFS, but overwrite instead of putIfAbsent — the last node seen per column wins.",
      },
      {
        q: "Vertical order traversal with ties (LC 987)?",
        a: "Sort by (column, row, value). Collect (col, row, val) triples then sort, or use a TreeMap<col, TreeMap<row, PriorityQueue<val>>>.",
      },
      {
        q: "Iterative max depth?",
        a: "BFS counting levels, or DFS with an explicit stack of (node, depth). Mention it when the interviewer says the tree can be 10^5 deep and skewed.",
      },
    ],
  },
  {
    id: "peak",
    t: "Find Peak Element",
    lc: 162,
    slug: "find-peak-element",
    d: "M",
    r: 1,
    src: ["S9"],
    stmt: "An element strictly greater than both neighbours is a peak; imagine arr[-1] = arr[n] = -infinity. Return the index of any peak in O(log n).",
    key: "The question is really 'can you binary search without a sorted array?'. The invariant: if arr[mid] < arr[mid+1] there must be a peak to the right, because the sequence is rising and must eventually fall (or hit the -infinity boundary).",
    brute: {
      i: "Linear scan for the first index bigger than both neighbours.",
      tc: "O(n)",
      sc: "O(1)",
      java: "int findPeakBrute(int[] a) {\n    for (int i = 0; i < a.length; i++) {\n        boolean leftOk  = (i == 0) || a[i] > a[i - 1];\n        boolean rightOk = (i == a.length - 1) || a[i] > a[i + 1];\n        if (leftOk && rightOk) return i;\n    }\n    return -1;\n}",
      py: "def find_peak_brute(a):\n    for i, v in enumerate(a):\n        left_ok = i == 0 or v > a[i - 1]\n        right_ok = i == len(a) - 1 or v > a[i + 1]\n        if left_ok and right_ok:\n            return i\n    return -1",
    },
    opt: {
      i: "Binary search on the slope. Compare arr[mid] with arr[mid+1]: if it is smaller, move lo = mid + 1 (climb right), otherwise hi = mid (a peak is at mid or to its left). The loop ends when lo == hi, which is a peak.",
      tc: "O(log n)",
      sc: "O(1)",
      java: "int findPeakElement(int[] a) {\n    int lo = 0, hi = a.length - 1;\n    while (lo < hi) {\n        int mid = lo + (hi - lo) / 2;\n        if (a[mid] < a[mid + 1]) lo = mid + 1;   // ascending: peak to the right\n        else hi = mid;                           // descending: peak here or left\n    }\n    return lo;\n}",
      py: "def find_peak_element(a):\n    lo, hi = 0, len(a) - 1\n    while lo < hi:\n        mid = (lo + hi) // 2\n        if a[mid] < a[mid + 1]:\n            lo = mid + 1\n        else:\n            hi = mid\n    return lo",
    },
    fu: [
      {
        q: "Why is binary search valid on unsorted data here?",
        a: "Because the decision rule is locally sufficient: from any slope direction you can guarantee a peak exists on that side (the boundary acts as -infinity). Binary search needs a monotone predicate, not a sorted array.",
      },
      {
        q: "2D peak (a peak greater than all four neighbours)?",
        a: "Binary search on columns: take the middle column, find its global maximum row, compare with the left/right neighbours, recurse into the larger side. O(m log n).",
      },
    ],
  },
  {
    id: "rotated",
    t: "Search in Rotated Sorted Array",
    lc: 33,
    slug: "search-in-rotated-sorted-array",
    d: "M",
    r: 1,
    src: ["S9"],
    stmt: "A sorted array was rotated at an unknown pivot. Find a target's index in O(log n), or -1.",
    key: "State the key fact first: at any mid, at least one half is fully sorted. Then it is bookkeeping. If you fumble, fall back to 'find the pivot with binary search, then binary search the correct half' — two clean searches read better than one messy one.",
    brute: {
      i: "Linear scan.",
      tc: "O(n)",
      sc: "O(1)",
      java: "int searchBrute(int[] a, int target) {\n    for (int i = 0; i < a.length; i++) if (a[i] == target) return i;\n    return -1;\n}",
      py: "def search_brute(a, target):\n    for i, v in enumerate(a):\n        if v == target:\n            return i\n    return -1",
    },
    opt: {
      i: "One-pass binary search: compute mid; if a[lo] <= a[mid] the left half is sorted, so check whether the target lies inside [a[lo], a[mid]) and move accordingly; otherwise the right half is sorted and you test (a[mid], a[hi]]. Use <= on the left comparison so the two-element case behaves.",
      tc: "O(log n)",
      sc: "O(1)",
      java: "int search(int[] a, int target) {\n    int lo = 0, hi = a.length - 1;\n    while (lo <= hi) {\n        int mid = lo + (hi - lo) / 2;\n        if (a[mid] == target) return mid;\n        if (a[lo] <= a[mid]) {                       // left half sorted\n            if (target >= a[lo] && target < a[mid]) hi = mid - 1;\n            else lo = mid + 1;\n        } else {                                     // right half sorted\n            if (target > a[mid] && target <= a[hi]) lo = mid + 1;\n            else hi = mid - 1;\n        }\n    }\n    return -1;\n}",
      py: "def search(a, target):\n    lo, hi = 0, len(a) - 1\n    while lo <= hi:\n        mid = (lo + hi) // 2\n        if a[mid] == target:\n            return mid\n        if a[lo] <= a[mid]:                # left sorted\n            if a[lo] <= target < a[mid]:\n                hi = mid - 1\n            else:\n                lo = mid + 1\n        else:                              # right sorted\n            if a[mid] < target <= a[hi]:\n                lo = mid + 1\n            else:\n                hi = mid - 1\n    return -1",
    },
    fu: [
      {
        q: "With duplicates (LC 81)?",
        a: "When a[lo] == a[mid] == a[hi] you cannot tell which side is sorted, so shrink with lo++ and hi--. Worst case degrades to O(n) — say that explicitly, it is the point of the follow-up.",
      },
      {
        q: "Find the rotation count / minimum element?",
        a: "Binary search comparing a[mid] with a[hi]: if a[mid] > a[hi] the minimum is right (lo = mid+1), else hi = mid. The final lo is the index of the minimum, which equals the number of rotations.",
      },
    ],
  },
  {
    id: "wordsearch",
    t: "Word Search",
    lc: 79,
    slug: "word-search",
    d: "M",
    r: 1,
    src: ["S9"],
    stmt: "Given a board of characters and a word, decide whether the word can be built from adjacent cells (horizontally or vertically), where a cell cannot be reused within one path.",
    key: "Backtracking with in-place marking. Two things earn points: mutating the board instead of carrying a visited set, and restoring the cell on the way out. If they follow up with many words, jump straight to a Trie (Word Search II).",
    brute: {
      i: "DFS from every cell carrying a HashSet of visited coordinates — correct but allocates a set per path and hashes on every step.",
      tc: "O(m·n·4^L)",
      sc: "O(L) plus the set",
      java: "boolean existBrute(char[][] b, String w) {\n    for (int i = 0; i < b.length; i++)\n        for (int j = 0; j < b[0].length; j++)\n            if (dfs(b, w, 0, i, j, new HashSet<>())) return true;\n    return false;\n}\nboolean dfs(char[][] b, String w, int k, int i, int j, Set<Integer> seen) {\n    if (k == w.length()) return true;\n    if (i < 0 || j < 0 || i >= b.length || j >= b[0].length) return false;\n    int id = i * b[0].length + j;\n    if (seen.contains(id) || b[i][j] != w.charAt(k)) return false;\n    seen.add(id);\n    boolean ok = dfs(b,w,k+1,i+1,j,seen) || dfs(b,w,k+1,i-1,j,seen)\n              || dfs(b,w,k+1,i,j+1,seen) || dfs(b,w,k+1,i,j-1,seen);\n    seen.remove(id);\n    return ok;\n}",
      py: "def exist_brute(b, w):\n    m, n = len(b), len(b[0])\n\n    def dfs(k, i, j, seen):\n        if k == len(w): return True\n        if not (0 <= i < m and 0 <= j < n): return False\n        if (i, j) in seen or b[i][j] != w[k]: return False\n        seen.add((i, j))\n        ok = (dfs(k+1, i+1, j, seen) or dfs(k+1, i-1, j, seen)\n              or dfs(k+1, i, j+1, seen) or dfs(k+1, i, j-1, seen))\n        seen.remove((i, j))\n        return ok\n\n    return any(dfs(0, i, j, set()) for i in range(m) for j in range(n))",
    },
    opt: {
      i: "Same backtracking, but mark the visited cell by overwriting it with a sentinel like '#' and restore it after the recursive calls. Add a cheap pruning guard: if the board's character counts cannot cover the word, return false immediately.",
      tc: "O(m·n·3^L) after the first step (you never go back the way you came)",
      sc: "O(L) recursion depth",
      java: "boolean exist(char[][] board, String word) {\n    char[] w = word.toCharArray();\n    for (int i = 0; i < board.length; i++)\n        for (int j = 0; j < board[0].length; j++)\n            if (dfs(board, w, 0, i, j)) return true;\n    return false;\n}\n\nprivate boolean dfs(char[][] b, char[] w, int k, int i, int j) {\n    if (k == w.length) return true;\n    if (i < 0 || j < 0 || i >= b.length || j >= b[0].length || b[i][j] != w[k]) return false;\n\n    char keep = b[i][j];\n    b[i][j] = '#';                                   // mark\n    boolean found = dfs(b, w, k + 1, i + 1, j) || dfs(b, w, k + 1, i - 1, j)\n                 || dfs(b, w, k + 1, i, j + 1) || dfs(b, w, k + 1, i, j - 1);\n    b[i][j] = keep;                                  // restore\n    return found;\n}",
      py: "def exist(board, word):\n    m, n = len(board), len(board[0])\n\n    def dfs(k, i, j):\n        if k == len(word): return True\n        if not (0 <= i < m and 0 <= j < n) or board[i][j] != word[k]:\n            return False\n        keep, board[i][j] = board[i][j], '#'\n        found = (dfs(k+1, i+1, j) or dfs(k+1, i-1, j)\n                 or dfs(k+1, i, j+1) or dfs(k+1, i, j-1))\n        board[i][j] = keep\n        return found\n\n    return any(dfs(0, i, j) for i in range(m) for j in range(n))",
    },
    fu: [
      {
        q: "Now search 10,000 words on the same board (LC 212).",
        a: "Build a Trie of all words and DFS the board once, walking the Trie in lockstep. Prune a branch as soon as the Trie has no child for the character, and delete words from the Trie once found so you do not report duplicates. This is the Trie question the bar raiser asks in round 3.",
      },
      {
        q: "Is the in-place marking safe with multiple threads?",
        a: "No — it mutates shared state. For a parallel run, either give each thread a copy of the board or go back to a per-thread visited bitset. Good answer to have ready — multithreading follow-ups are common on this one.",
      },
    ],
  },
  {
    id: "threesum",
    t: "3Sum — without using a hash map",
    lc: 15,
    slug: "3sum",
    d: "M",
    r: 1,
    src: ["S9"],
    stmt: "Return all unique triplets that sum to zero. The interviewer explicitly banned hash maps.",
    key: "Banning the map forces sort + two pointers. Dedup discipline is the whole grade: skip duplicates at the fixed index and after each successful pair.",
    brute: {
      i: "Three nested loops, deduplicate the triplets with a set of sorted triples.",
      tc: "O(n^3)",
      sc: "O(k) for the result set",
      java: "List<List<Integer>> threeSumBrute(int[] a) {\n    Arrays.sort(a);\n    Set<List<Integer>> set = new LinkedHashSet<>();\n    for (int i = 0; i < a.length; i++)\n        for (int j = i + 1; j < a.length; j++)\n            for (int k = j + 1; k < a.length; k++)\n                if (a[i] + a[j] + a[k] == 0)\n                    set.add(Arrays.asList(a[i], a[j], a[k]));\n    return new ArrayList<>(set);\n}",
      py: "def three_sum_brute(a):\n    a.sort()\n    out = set()\n    n = len(a)\n    for i in range(n):\n        for j in range(i + 1, n):\n            for k in range(j + 1, n):\n                if a[i] + a[j] + a[k] == 0:\n                    out.add((a[i], a[j], a[k]))\n    return [list(t) for t in out]",
    },
    opt: {
      i: "Sort. Fix index i, then run two pointers lo = i+1, hi = n-1 over the remainder. Skip a repeated a[i], and after recording a triplet advance both pointers past their duplicates. Early exit when a[i] > 0 because the array is sorted.",
      tc: "O(n^2)",
      sc: "O(1) beyond the output (sorting in place)",
      java: "List<List<Integer>> threeSum(int[] a) {\n    Arrays.sort(a);\n    List<List<Integer>> out = new ArrayList<>();\n    for (int i = 0; i < a.length - 2; i++) {\n        if (a[i] > 0) break;                       // no way to reach 0 anymore\n        if (i > 0 && a[i] == a[i - 1]) continue;   // skip duplicate anchor\n        int lo = i + 1, hi = a.length - 1;\n        while (lo < hi) {\n            int sum = a[i] + a[lo] + a[hi];\n            if (sum == 0) {\n                out.add(Arrays.asList(a[i], a[lo], a[hi]));\n                while (lo < hi && a[lo] == a[lo + 1]) lo++;\n                while (lo < hi && a[hi] == a[hi - 1]) hi--;\n                lo++; hi--;\n            } else if (sum < 0) lo++;\n            else hi--;\n        }\n    }\n    return out;\n}",
      py: "def three_sum(a):\n    a.sort()\n    out = []\n    n = len(a)\n    for i in range(n - 2):\n        if a[i] > 0:\n            break\n        if i and a[i] == a[i - 1]:\n            continue\n        lo, hi = i + 1, n - 1\n        while lo < hi:\n            s = a[i] + a[lo] + a[hi]\n            if s == 0:\n                out.append([a[i], a[lo], a[hi]])\n                while lo < hi and a[lo] == a[lo + 1]: lo += 1\n                while lo < hi and a[hi] == a[hi - 1]: hi -= 1\n                lo += 1; hi -= 1\n            elif s < 0:\n                lo += 1\n            else:\n                hi -= 1\n    return out",
    },
    fu: [
      {
        q: "4Sum?",
        a: "Two nested anchors plus two pointers, O(n^3), with the same dedup rules at both anchor levels. Generalise to kSum with recursion that bottoms out at the two-pointer case.",
      },
      {
        q: "3Sum closest?",
        a: "Same scan, but track the triplet whose sum has the smallest absolute difference from the target instead of requiring equality.",
      },
      {
        q: "Why did they ban the hash map?",
        a: "To stop the O(n^2) 'two sum with a set' solution and force you to demonstrate the pointer-and-dedup mechanics. Sorting also gives O(1) extra space, which is the better answer when memory is constrained.",
      },
    ],
  },
  {
    id: "diameter",
    t: "Diameter of Binary Tree",
    lc: 543,
    slug: "diameter-of-binary-tree",
    d: "E",
    r: 1,
    src: ["S9"],
    stmt: "Return the length of the longest path between any two nodes, measured in edges. The path need not pass through the root.",
    key: "The pattern — a recursion that returns one thing (height) while updating another via a field (the best diameter) — recurs in max path sum, longest univalue path and balanced-tree checks. Name the pattern.",
    brute: {
      i: "For every node compute height(left) + height(right) and take the maximum. Height is recomputed for every node.",
      tc: "O(n^2) on a skewed tree",
      sc: "O(h)",
      java: "int diameterBrute(TreeNode root) {\n    if (root == null) return 0;\n    int through = height(root.left) + height(root.right);\n    return Math.max(through, Math.max(diameterBrute(root.left), diameterBrute(root.right)));\n}\nint height(TreeNode n) {\n    return n == null ? 0 : 1 + Math.max(height(n.left), height(n.right));\n}",
      py: "def diameter_brute(root):\n    if not root: return 0\n    def height(n):\n        return 0 if not n else 1 + max(height(n.left), height(n.right))\n    through = height(root.left) + height(root.right)\n    return max(through, diameter_brute(root.left), diameter_brute(root.right))",
    },
    opt: {
      i: "One post-order pass. The recursion returns the height of the subtree; while returning, it updates a member field with left + right (the path through this node). Each node is visited once.",
      tc: "O(n)",
      sc: "O(h)",
      java: "class Solution {\n    private int best = 0;\n\n    public int diameterOfBinaryTree(TreeNode root) {\n        height(root);\n        return best;\n    }\n\n    private int height(TreeNode n) {\n        if (n == null) return 0;\n        int l = height(n.left), r = height(n.right);\n        best = Math.max(best, l + r);       // path through n, counted in edges\n        return 1 + Math.max(l, r);\n    }\n}",
      py: "def diameter_of_binary_tree(root):\n    best = 0\n\n    def height(n):\n        nonlocal best\n        if not n:\n            return 0\n        l, r = height(n.left), height(n.right)\n        best = max(best, l + r)\n        return 1 + max(l, r)\n\n    height(root)\n    return best",
    },
    fu: [
      {
        q: "Return the actual path, not just the length?",
        a: "Have the recursion return (height, pathList) or store parent pointers and, once you know the node where the best split happened, walk down the deepest branch on each side.",
      },
      {
        q: "Maximum path sum (LC 124)?",
        a: "Same shape: the recursion returns max(0, node.val + max(left, right)) so negative branches are dropped, and the field is updated with node.val + leftGain + rightGain.",
      },
      {
        q: "Very deep tree?",
        a: "Convert to an explicit stack post-order traversal to avoid StackOverflowError — this kind of production-realism follow-up is standard in a bar-raiser round.",
      },
    ],
  },
  {
    id: "majority",
    t: "Majority Element",
    lc: 169,
    slug: "majority-element",
    d: "E",
    r: 1,
    src: ["S9"],
    stmt: "Find the element that appears more than n/2 times. Assume it exists; then remove that assumption.",
    key: "Boyer-Moore voting in O(1) space is the expected answer, and the follow-up 'what if no majority exists' (verify with a second pass) is what separates a rehearsed answer from an understood one.",
    brute: {
      i: "Count occurrences in a hash map and return the key whose count exceeds n/2. Or sort and return the middle element.",
      tc: "O(n) with O(n) space, or O(n log n) sorting",
      sc: "O(n) / O(1)",
      java: "int majorityBrute(int[] a) {\n    Map<Integer, Integer> count = new HashMap<>();\n    for (int v : a) {\n        int c = count.merge(v, 1, Integer::sum);\n        if (c > a.length / 2) return v;\n    }\n    return -1;\n}",
      py: "from collections import Counter\n\ndef majority_brute(a):\n    c = Counter(a)\n    for v, n in c.items():\n        if n > len(a) // 2:\n            return v\n    return -1",
    },
    opt: {
      i: "Boyer-Moore: keep a candidate and a counter. Same value increments, different value decrements; at zero, adopt the current value as the new candidate. A majority element survives because it outnumbers everything else combined. Verify with a second pass if existence is not guaranteed.",
      tc: "O(n)",
      sc: "O(1)",
      java: "int majorityElement(int[] a) {\n    int candidate = a[0], count = 0;\n    for (int v : a) {\n        if (count == 0) candidate = v;\n        count += (v == candidate) ? 1 : -1;\n    }\n    // verification pass, needed when a majority is not guaranteed\n    int freq = 0;\n    for (int v : a) if (v == candidate) freq++;\n    return freq > a.length / 2 ? candidate : -1;\n}",
      py: "def majority_element(a):\n    candidate, count = a[0], 0\n    for v in a:\n        if count == 0:\n            candidate = v\n        count += 1 if v == candidate else -1\n    return candidate if a.count(candidate) > len(a) // 2 else -1",
    },
    fu: [
      {
        q: "Elements appearing more than n/3 times (LC 229)?",
        a: "Boyer-Moore generalises: keep 2 candidates and 2 counters (at most two values can exceed n/3), then verify both. For n/k, keep k-1 candidates — the Misra-Gries summary.",
      },
      {
        q: "Over a stream too large for memory?",
        a: "Misra-Gries or Count-Min Sketch gives approximate heavy hitters in sublinear space; exact answers need a second pass over the data.",
      },
    ],
  },
  {
    id: "boundary",
    t: "Boundary of Binary Tree",
    lc: 545,
    slug: "boundary-of-binary-tree",
    d: "M",
    r: 1,
    src: ["S9"],
    stmt: "Print the boundary anti-clockwise: root, then the left boundary top-down (excluding leaves), then all leaves left-to-right, then the right boundary bottom-up (excluding leaves).",
    key: "Not algorithmically hard, but full of edge cases — a root with a single child, leaves that also sit on a boundary, a single-node tree. The interviewer is testing careful case analysis, so enumerate the cases before coding.",
    brute: {
      i: "Collect the three parts with separate traversals into lists, then concatenate and remove duplicates by identity. Works, but the dedup step is a smell.",
      tc: "O(n)",
      sc: "O(n)",
      java: "// collect leftBoundary, leaves, rightBoundary into lists, concatenate,\n// then drop duplicates where a node appears in two lists (leaf on a boundary).\n// Correct, but the duplicate removal shows the rules were not thought through.",
      py: "# same idea: three traversals, concatenate, then de-duplicate by node identity",
    },
    opt: {
      i: "Three helpers with a single rule: a node counts as a leaf if it has no children, and leaves are excluded from the boundary walks. Add the root first (unless it is a leaf), walk the left boundary top-down preferring left then right child, collect leaves with an in-order DFS, then walk the right boundary and reverse it.",
      tc: "O(n)",
      sc: "O(h)",
      java: "List<Integer> boundaryOfBinaryTree(TreeNode root) {\n    List<Integer> out = new ArrayList<>();\n    if (root == null) return out;\n    if (!isLeaf(root)) out.add(root.val);\n\n    // left boundary, excluding leaves\n    TreeNode cur = root.left;\n    while (cur != null) {\n        if (!isLeaf(cur)) out.add(cur.val);\n        cur = (cur.left != null) ? cur.left : cur.right;\n    }\n\n    addLeaves(root, out);\n\n    // right boundary, excluding leaves, collected then reversed\n    List<Integer> right = new ArrayList<>();\n    cur = root.right;\n    while (cur != null) {\n        if (!isLeaf(cur)) right.add(cur.val);\n        cur = (cur.right != null) ? cur.right : cur.left;\n    }\n    Collections.reverse(right);\n    out.addAll(right);\n    return out;\n}\n\nprivate boolean isLeaf(TreeNode n) { return n.left == null && n.right == null; }\n\nprivate void addLeaves(TreeNode n, List<Integer> out) {\n    if (n == null) return;\n    if (isLeaf(n)) { out.add(n.val); return; }\n    addLeaves(n.left, out);\n    addLeaves(n.right, out);\n}",
      py: "def boundary_of_binary_tree(root):\n    if not root:\n        return []\n    is_leaf = lambda n: not n.left and not n.right\n    out = [] if is_leaf(root) else [root.val]\n\n    cur = root.left\n    while cur:\n        if not is_leaf(cur):\n            out.append(cur.val)\n        cur = cur.left or cur.right\n\n    def leaves(n):\n        if not n: return\n        if is_leaf(n):\n            out.append(n.val); return\n        leaves(n.left); leaves(n.right)\n    leaves(root)\n\n    right = []\n    cur = root.right\n    while cur:\n        if not is_leaf(cur):\n            right.append(cur.val)\n        cur = cur.right or cur.left\n    out.extend(reversed(right))\n    return out",
    },
    fu: [
      {
        q: "What if the root is a leaf?",
        a: "Return just [root.val]. The guard `if (!isLeaf(root))` plus addLeaves handles it — walk through this case aloud, it is the one candidates get wrong.",
      },
      {
        q: "Root has only a left child — is that child on the left boundary or the right boundary?",
        a: "By convention the left boundary; the right walk starts at root.right, which is null, so it contributes nothing. Confirm the convention with the interviewer rather than assuming.",
      },
    ],
  },
  {
    id: "lswr",
    t: "Longest Substring Without Repeating Characters",
    lc: 3,
    slug: "longest-substring-without-repeating-characters",
    d: "M",
    r: 1,
    src: ["S8"],
    stmt: "Return the length of the longest substring with all distinct characters.",
    key: "The canonical sliding window. The version that scores highest jumps the left pointer straight to lastIndex+1 instead of shrinking one character at a time — mention that it makes each index move at most once.",
    brute: {
      i: "Check every substring for uniqueness with a set.",
      tc: "O(n^3) naive, O(n^2) with an incremental set",
      sc: "O(min(n, charset))",
      java: "int lengthOfLongestBrute(String s) {\n    int best = 0;\n    for (int i = 0; i < s.length(); i++) {\n        Set<Character> seen = new HashSet<>();\n        int j = i;\n        while (j < s.length() && seen.add(s.charAt(j))) j++;\n        best = Math.max(best, j - i);\n    }\n    return best;\n}",
      py: "def length_of_longest_brute(s):\n    best = 0\n    for i in range(len(s)):\n        seen = set()\n        j = i\n        while j < len(s) and s[j] not in seen:\n            seen.add(s[j]); j += 1\n        best = max(best, j - i)\n    return best",
    },
    opt: {
      i: "Sliding window with a map from character to its last index. When the current character was seen at index >= left, jump left to lastIndex + 1 in one step. Each index is visited once by each pointer.",
      tc: "O(n)",
      sc: "O(min(n, charset))",
      java: "int lengthOfLongestSubstring(String s) {\n    Map<Character, Integer> last = new HashMap<>();\n    int left = 0, best = 0;\n    for (int right = 0; right < s.length(); right++) {\n        char c = s.charAt(right);\n        Integer prev = last.get(c);\n        if (prev != null && prev >= left) left = prev + 1;   // jump, do not crawl\n        last.put(c, right);\n        best = Math.max(best, right - left + 1);\n    }\n    return best;\n}",
      py: "def length_of_longest_substring(s):\n    last = {}\n    left = best = 0\n    for right, c in enumerate(s):\n        if c in last and last[c] >= left:\n            left = last[c] + 1\n        last[c] = right\n        best = max(best, right - left + 1)\n    return best",
    },
    fu: [
      {
        q: "Return the substring itself?",
        a: "Track bestStart alongside bestLength and slice at the end.",
      },
      {
        q: "At most K distinct characters (LC 340)?",
        a: "Same window with a count map; shrink from the left while map.size() > K. This family — longest with at most K distinct, minimum window substring, permutation in string — is one template.",
      },
      {
        q: "Unicode input?",
        a: "Iterate code points, not chars: a Java char is a UTF-16 unit, so emoji and other supplementary characters occupy two chars. Use s.codePoints() or offsetByCodePoints.",
      },
    ],
  },
  {
    id: "binsearchback",
    t: "Binary search that needs backtracking (split array largest sum)",
    lc: 410,
    slug: "split-array-largest-sum",
    d: "H",
    r: 1,
    src: ["S7"],
    stmt: "Reported as 'two Medium binary-search questions related to backtracking'. The representative problem: split an array into k contiguous subarrays so the largest subarray sum is minimised.",
    key: "This is binary search on the answer, with a greedy feasibility check inside — the pattern behind Koko eating bananas, ship packages in D days, and minimum capacity problems. The moment you say 'I will binary search the answer, not the index', the interviewer relaxes.",
    brute: {
      i: "Backtrack over every way to place k-1 cuts, or DP: dp[i][k] = min over j of max(dp[j][k-1], sum(j..i)).",
      tc: "Exponential backtracking; O(n^2·k) with DP",
      sc: "O(n·k)",
      java: "int splitArrayDP(int[] a, int k) {\n    int n = a.length;\n    long[] pre = new long[n + 1];\n    for (int i = 0; i < n; i++) pre[i + 1] = pre[i] + a[i];\n\n    long[][] dp = new long[k + 1][n + 1];\n    for (long[] row : dp) Arrays.fill(row, Long.MAX_VALUE / 4);\n    dp[0][0] = 0;\n    for (int parts = 1; parts <= k; parts++)\n        for (int i = 1; i <= n; i++)\n            for (int j = parts - 1; j < i; j++)\n                dp[parts][i] = Math.min(dp[parts][i],\n                        Math.max(dp[parts - 1][j], pre[i] - pre[j]));\n    return (int) dp[k][n];\n}",
      py: "def split_array_dp(a, k):\n    n = len(a)\n    pre = [0] * (n + 1)\n    for i, v in enumerate(a):\n        pre[i + 1] = pre[i] + v\n    INF = float('inf')\n    dp = [[INF] * (n + 1) for _ in range(k + 1)]\n    dp[0][0] = 0\n    for parts in range(1, k + 1):\n        for i in range(1, n + 1):\n            for j in range(parts - 1, i):\n                dp[parts][i] = min(dp[parts][i], max(dp[parts-1][j], pre[i]-pre[j]))\n    return dp[k][n]",
    },
    opt: {
      i: "Binary search the answer between max(a) and sum(a). For a candidate limit, greedily walk the array opening a new subarray whenever adding the next element would exceed the limit; if the number of subarrays needed is <= k, the limit is feasible, so search lower. Feasibility is monotone, which is what makes binary search legal — state that.",
      tc: "O(n · log(sum))",
      sc: "O(1)",
      java: "int splitArray(int[] a, int k) {\n    long lo = 0, hi = 0;\n    for (int v : a) { lo = Math.max(lo, v); hi += v; }\n\n    while (lo < hi) {\n        long mid = lo + (hi - lo) / 2;\n        if (feasible(a, k, mid)) hi = mid;   // try a tighter limit\n        else lo = mid + 1;\n    }\n    return (int) lo;\n}\n\nprivate boolean feasible(int[] a, int k, long limit) {\n    int parts = 1; long running = 0;\n    for (int v : a) {\n        if (running + v > limit) { parts++; running = v; }\n        else running += v;\n        if (parts > k) return false;\n    }\n    return true;\n}",
      py: "def split_array(a, k):\n    lo, hi = max(a), sum(a)\n\n    def feasible(limit):\n        parts, running = 1, 0\n        for v in a:\n            if running + v > limit:\n                parts += 1\n                running = v\n                if parts > k:\n                    return False\n            else:\n                running += v\n        return True\n\n    while lo < hi:\n        mid = (lo + hi) // 2\n        if feasible(mid):\n            hi = mid\n        else:\n            lo = mid + 1\n    return lo",
    },
    fu: [
      {
        q: "Which problems share this template?",
        a: "Koko Eating Bananas (875), Capacity to Ship Packages (1011), Minimum Number of Days to Make Bouquets (1482), Aggressive Cows. All are: binary search the answer + a linear greedy feasibility check.",
      },
      {
        q: "How do you know binary search is valid?",
        a: "Because feasible(x) is monotone — if a limit works, every larger limit works. Prove monotonicity aloud; that is the step interviewers actually grade.",
      },
      {
        q: "They said 'backtracking' — should I write the backtracking version at all?",
        a: "Write the feasibility greedy first, then mention the DP/backtracking formulation as the exhaustive baseline and why you are not using it (exponential / O(n^2 k)). Showing both and choosing is stronger than only knowing one.",
      },
    ],
  },
  {
    id: "sort01",
    t: "Sort an array of 0s and 1s (and the 0/1/2 follow-up)",
    lc: 75,
    slug: "sort-colors",
    d: "E",
    r: 1,
    src: ["S5"],
    stmt: "Sort an array containing only 0s and 1s in place, in one pass. Follow-up: 0s, 1s and 2s (Dutch national flag).",
    key: "Say 'counting sort in two passes, or two pointers in one pass' and then ask which constraint matters — one pass or constant space. The 0/1/2 follow-up is near-certain, so go straight to the three-pointer partition.",
    brute: {
      i: "Count the zeros, then overwrite the array with that many zeros followed by ones. Two passes, O(1) space.",
      tc: "O(n), two passes",
      sc: "O(1)",
      java: "void sortBinaryCount(int[] a) {\n    int zeros = 0;\n    for (int v : a) if (v == 0) zeros++;\n    for (int i = 0; i < a.length; i++) a[i] = (i < zeros) ? 0 : 1;\n}",
      py: "def sort_binary_count(a):\n    zeros = a.count(0)\n    for i in range(len(a)):\n        a[i] = 0 if i < zeros else 1\n    return a",
    },
    opt: {
      i: "One pass, two pointers: low marks the next slot for a 0, high the next slot for a 1; swap and move. For 0/1/2 use the Dutch national flag partition with low, mid and high — note that when you swap with high you must not advance mid, because the value you swapped in has not been examined yet.",
      tc: "O(n), one pass",
      sc: "O(1)",
      java: "void sortBinary(int[] a) {\n    int low = 0, high = a.length - 1;\n    while (low < high) {\n        if (a[low] == 0) low++;\n        else if (a[high] == 1) high--;\n        else { a[low++] = 0; a[high--] = 1; }\n    }\n}\n\n// Dutch national flag: 0s, 1s, 2s\nvoid sortColors(int[] a) {\n    int low = 0, mid = 0, high = a.length - 1;\n    while (mid <= high) {\n        if (a[mid] == 0)      { swap(a, low++, mid++); }\n        else if (a[mid] == 1) { mid++; }\n        else                  { swap(a, mid, high--); }   // do NOT advance mid\n    }\n}\nprivate void swap(int[] a, int i, int j) { int t = a[i]; a[i] = a[j]; a[j] = t; }",
      py: "def sort_binary(a):\n    low, high = 0, len(a) - 1\n    while low < high:\n        if a[low] == 0:\n            low += 1\n        elif a[high] == 1:\n            high -= 1\n        else:\n            a[low], a[high] = 0, 1\n            low += 1; high -= 1\n    return a\n\ndef sort_colors(a):\n    low = mid = 0\n    high = len(a) - 1\n    while mid <= high:\n        if a[mid] == 0:\n            a[low], a[mid] = a[mid], a[low]\n            low += 1; mid += 1\n        elif a[mid] == 1:\n            mid += 1\n        else:\n            a[mid], a[high] = a[high], a[mid]\n            high -= 1          # mid stays put\n    return a",
    },
    fu: [
      {
        q: "Why must mid not advance after swapping with high?",
        a: "The element swapped in from the back has never been inspected — it could be a 0 that still needs to go left. Advancing mid would skip it. This exact question is the follow-up.",
      },
      {
        q: "K colours instead of 3?",
        a: "Counting sort in O(n + k), two passes. In-place k-way partition needs O(n log k) or repeated 3-way partitions.",
      },
    ],
  },
  {
    id: "intersect",
    t: "Intersection of Two Linked Lists",
    lc: 160,
    slug: "intersection-of-two-linked-lists",
    d: "E",
    r: 1,
    src: ["S6"],
    stmt: "Two singly linked lists may merge at some node. Return the first common node, or null. Do it in O(1) extra space.",
    key: "The two-pointer switch trick is the expected answer, and the reason it works — both pointers travel lenA + lenB before meeting — is the actual question. Offer the length-difference version too: it is easier to explain under pressure.",
    brute: {
      i: "Put every node of list A in a HashSet by identity, then walk B and return the first node already in the set.",
      tc: "O(m + n)",
      sc: "O(m)",
      java: "ListNode intersectionBrute(ListNode a, ListNode b) {\n    Set<ListNode> seen = Collections.newSetFromMap(new IdentityHashMap<>());\n    for (ListNode n = a; n != null; n = n.next) seen.add(n);\n    for (ListNode n = b; n != null; n = n.next) if (seen.contains(n)) return n;\n    return null;\n}",
      py: "def intersection_brute(a, b):\n    seen = set()\n    while a:\n        seen.add(id(a)); a = a.next\n    while b:\n        if id(b) in seen:\n            return b\n        b = b.next\n    return None",
    },
    opt: {
      i: "Two pointers: walk pA over A then continue into B, and pB over B then into A. Both cover exactly lenA + lenB nodes, so they arrive at the intersection together — or both reach null if there is none. Alternative: measure both lengths, advance the longer list by the difference, then walk in step.",
      tc: "O(m + n)",
      sc: "O(1)",
      java: "ListNode getIntersectionNode(ListNode headA, ListNode headB) {\n    if (headA == null || headB == null) return null;\n    ListNode pA = headA, pB = headB;\n    while (pA != pB) {                     // meets at the node, or both at null\n        pA = (pA == null) ? headB : pA.next;\n        pB = (pB == null) ? headA : pB.next;\n    }\n    return pA;\n}",
      py: "def get_intersection_node(head_a, head_b):\n    if not head_a or not head_b:\n        return None\n    pa, pb = head_a, head_b\n    while pa is not pb:\n        pa = head_b if pa is None else pa.next\n        pb = head_a if pb is None else pb.next\n    return pa",
    },
    fu: [
      {
        q: "Why does the switch trick terminate?",
        a: "Let a be the length before the junction in A, b the same in B, and c the shared tail. pA walks a + c + b and pB walks b + c + a — equal — so they land on the junction simultaneously. With no intersection, both walk m + n and hit null together.",
      },
      {
        q: "What if the lists have cycles?",
        a: "Detect cycles with Floyd first. If exactly one has a cycle they cannot intersect; if both do, check whether one's cycle entry is reachable from the other's.",
      },
      {
        q: "Equal values but different nodes?",
        a: "Compare by reference, not by value. That is why the brute force uses IdentityHashMap — worth calling out.",
      },
    ],
  },
];
