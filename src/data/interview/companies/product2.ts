// Imported from the Interview Prep Console (companies-product2.js).
import type { Company } from "../types";

export const product2Companies: Company[] = [
  {
    id: "uber",
    name: "Uber",
    sector: "product",
    tag: "Domain-specialised coding plus a technical retrospective for senior candidates.",
    loc: "Bengaluru · Hyderabad",
    ladder: [
      {
        lvl: "SDE",
        internal: "L3 / SDE I",
        yoe: "0–2",
        loop: "OA + 3 rounds",
        bar: "Clean DSA, basic API design.",
      },
      {
        lvl: "SDE2",
        internal: "L4",
        yoe: "2–5",
        loop: "Phone screen + 4 onsite",
        bar: "General + domain coding, one design round, ownership stories.",
      },
      {
        lvl: "Lead SDE",
        internal: "L5A (Senior)",
        yoe: "5–9",
        loop: "Phone screen + 5–6 onsite incl. technical retrospective",
        bar: "Distributed design with real numbers; the retrospective round dissects a system you built.",
      },
      {
        lvl: "Staff SDE",
        internal: "L5B / Staff",
        yoe: "9–13",
        loop: "Two design rounds + retrospective + leadership",
        bar: "Cross-org architecture, migration strategy, on-call and cost ownership.",
      },
      {
        lvl: "Principal SDE",
        internal: "L6+",
        yoe: "13+",
        loop: "Architecture panel with directors",
        bar: "Platform direction across business lines.",
      },
    ],
    loop: [
      {
        n: 1,
        name: "Technical phone screen",
        dur: "45–60 min",
        fmt: "CodeSignal / CoderPad",
        tests: "One or two Medium problems.",
        levels: ["SDE", "SDE2", "Lead SDE", "Staff SDE"],
      },
      {
        n: 2,
        name: "General coding",
        dur: "60 min",
        fmt: "Live coding",
        tests: "Standard DSA — graphs, heaps, intervals.",
        levels: ["SDE", "SDE2", "Lead SDE", "Staff SDE"],
      },
      {
        n: 3,
        name: "Specialised coding (domain)",
        dur: "60 min",
        fmt: "Live coding",
        tests:
          "Problems shaped like Uber's domain: geospatial matching, trip state, concurrency, or language-specific work for the team you'd join.",
        levels: ["SDE2", "Lead SDE", "Staff SDE"],
      },
      {
        n: 4,
        name: "System design",
        dur: "60 min",
        fmt: "Whiteboard",
        tests:
          "Location, dispatch, payments, real-time streaming. Failure and consistency discussion is heavy.",
        levels: ["SDE2", "Lead SDE", "Staff SDE", "Principal SDE"],
      },
      {
        n: 5,
        name: "Technical retrospective",
        dur: "45–60 min",
        fmt: "Conversation",
        tests:
          "Senior only. Pick a system you built: why those choices, what broke, what you would change. Effectively a design round about your own past.",
        levels: ["Lead SDE", "Staff SDE", "Principal SDE"],
      },
      {
        n: 6,
        name: "Behavioural / hiring manager",
        dur: "45 min",
        fmt: "Conversation",
        tests: "Collaboration, disagreement with manager, a project that failed.",
        levels: ["SDE", "SDE2", "Lead SDE", "Staff SDE", "Principal SDE"],
      },
    ],
    bank: [
      {
        q: "Design Uber / a ride-matching system",
        t: "hld",
        lv: ["SDE2", "Lead SDE", "Staff SDE"],
        d: "H",
        round: 4,
        ans: "hld-uber",
        note: "Their own domain — expect deeper probing on the geo index, batched matching and the exactly-one-assignment guarantee than anywhere else.",
      },
      {
        q: "Design WhatsApp / a chat system",
        t: "hld",
        lv: ["Lead SDE", "Staff SDE"],
        d: "H",
        round: 4,
        ans: "hld-whatsapp",
      },
      {
        q: "Design Instagram",
        t: "hld",
        lv: ["SDE2", "Lead SDE"],
        d: "H",
        round: 4,
        ans: "hld-instagram",
      },
      {
        q: "Design an online chess game",
        t: "lld",
        lv: ["SDE2", "Lead SDE"],
        d: "M",
        round: 3,
        a: "**Clarify**: real-time or correspondence, matchmaking in scope, spectators, move validation server-side (yes — never trust the client), and whether you need full rules (castling, en passant, promotion, threefold repetition).\n\n**Model**: `Game` (id, players, `Board`, turn, state machine ACTIVE→CHECK→CHECKMATE/STALEMATE/DRAW/RESIGNED), `Board` (8×8 `Square[]`, plus move history for repetition and en passant), `Piece` abstract with `Pawn/Rook/Knight/Bishop/Queen/King` each implementing `legalMoves(board, from)` — the Strategy/polymorphism axis that makes this question worth asking. `MoveValidator` layers the global rules on top (cannot leave your own king in check), `GameService` applies moves and emits events.\n\n**Key design points**: moves are validated as `pseudo-legal` per piece, then filtered by simulating the move and testing check; the move history makes undo, en passant and repetition detection possible; a `Move` is a value object (from, to, piece, captured, promotion, flags) so the whole game is replayable from a list of moves.\n\n**Real-time**: WebSocket per game room, server authoritative, with a move sequence number so a reconnecting client resyncs from its last seen index.",
        fu: [
          {
            q: "How do you handle a player's clock?",
            a: "Server-side clocks only — start the opponent's timer when a move is accepted, and store the remaining millis on the game. Never trust client timestamps.",
          },
          {
            q: "Scale to a million concurrent games?",
            a: "Games are independent, so shard by game id across stateful nodes with a routing registry, exactly like the chat gateway design. Persist moves append-only so a node can be rebuilt anywhere.",
          },
        ],
      },
      {
        q: "Sliding window / vowel substring counting",
        t: "coding",
        lv: ["SDE", "SDE2"],
        d: "M",
        round: 2,
        ans: "lswr",
        note: "Uber's version counts substrings made only of vowels containing all five — the same expand/contract window with a counter of distinct vowels.",
      },
      {
        q: "Meeting rooms / interval merging under a constraint",
        t: "coding",
        lv: ["SDE", "SDE2", "Lead SDE"],
        d: "M",
        lc: 253,
        slug: "meeting-rooms-ii",
        round: 2,
        ans: "meetingrooms",
      },
      {
        q: "Implement Promise.all (JavaScript roles)",
        t: "coding",
        lv: ["SDE2", "Lead SDE"],
        d: "M",
        round: 3,
        a: "Return a new Promise; iterate the iterable keeping the index; resolve each entry with `Promise.resolve(x).then(...)` so non-promise values work; write results into a pre-sized array **by index** (order must match input, not completion); keep a pending counter and resolve when it hits zero. Reject immediately on the first rejection — later rejections are ignored because a settled promise cannot change.\n\nEdge cases they check: empty input resolves immediately with `[]`; a non-promise value passes through; and you must not use `push` (that would order by completion).",
        codeLang: "JavaScript",
        code: "function promiseAll(items) {\n  return new Promise((resolve, reject) => {\n    const values = new Array(items.length);\n    let pending = items.length;\n    if (pending === 0) return resolve(values);   // empty input\n\n    items.forEach((item, i) => {\n      Promise.resolve(item).then(\n        (value) => {\n          values[i] = value;                     // by index, not push\n          if (--pending === 0) resolve(values);\n        },\n        reject                                   // first rejection wins\n      );\n    });\n  });\n}\n\n// allSettled variant: never rejects, wraps each outcome\nfunction promiseAllSettled(items) {\n  return promiseAll(items.map((p) =>\n    Promise.resolve(p).then(\n      (value) => ({ status: 'fulfilled', value }),\n      (reason) => ({ status: 'rejected', reason })\n    )));\n}",
      },
      {
        q: "Tell me about a disagreement with your manager",
        t: "behavioral",
        lv: ["SDE2", "Lead SDE", "Staff SDE"],
        round: 6,
        a: "Uber asks this one almost verbatim. The safe shape: a decision where you had information your manager did not (a performance number, a customer signal), you raised it privately with evidence, you proposed an alternative with its cost, and then you either changed their mind or committed to their call and made it work. Say which — both endings are acceptable, but only if you can articulate why.\n\nRed flags: framing your manager as incompetent, or a story where you quietly did it your own way anyway.",
      },
      {
        q: "Technical retrospective: walk me through a system you built and what you would change",
        t: "behavioral",
        lv: ["Lead SDE", "Staff SDE", "Principal SDE"],
        round: 5,
        ans: "hld-ownsystem",
        note: "Uber makes this a dedicated round. The library card is the full preparation checklist — diagram, four numbers, two decisions, one incident, one improvement.",
      },
    ],
    prep: [
      "Prepare the technical retrospective like a talk: one architecture you can draw in three minutes with real traffic numbers.",
      "Their design questions come from their own domain — ride matching, ETA, surge, payments — so know geo indexing (H3/geohash) and dispatch.",
      "The specialised coding round matches the team: ask your recruiter which domain it will cover.",
    ],
    sources: [
      {
        label: "Exponent — Uber SWE interview guide",
        url: "https://www.tryexponent.com/guides/uber-software-engineer-interview",
      },
      {
        label: "LeetCode — Uber L5A SSE interview experience",
        url: "https://leetcode.com/discuss/interview-experience/7043175/",
      },
      {
        label: "Medium — Uber L5A offer experience",
        url: "https://medium.com/@rajatgoyal715/my-interview-experience-at-uber-l5a-offer-sr-software-engineer-c8f9fc8451b7",
      },
    ],
  },
  {
    id: "atlassian",
    name: "Atlassian",
    sector: "product",
    tag: "Two 'code design' rounds — working code plus an extension — and a values round that rejects people.",
    loc: "Bengaluru",
    ladder: [
      {
        lvl: "SDE",
        internal: "P4",
        yoe: "0–3",
        loop: "OA + 2 code design + values",
        bar: "Working solution to the base problem, readable code.",
      },
      {
        lvl: "SDE2",
        internal: "P4/P5",
        yoe: "3–6",
        loop: "2 code design + system design + HM + values",
        bar: "Solve both code design problems **with** their extensions — reports say this is the offer threshold.",
      },
      {
        lvl: "Lead SDE",
        internal: "P50 (Senior)",
        yoe: "6–9",
        loop: "2 code design + system design + HM + values",
        bar: "REST API design quality, extensible object model, scale discussion.",
      },
      {
        lvl: "Staff SDE",
        internal: "P60 (Principal)",
        yoe: "9–14",
        loop: "System design ×2 + architecture deep dive + values",
        bar: "Platform thinking across products, technical leadership without authority.",
      },
      {
        lvl: "Principal SDE",
        internal: "P70",
        yoe: "14+",
        loop: "Architecture + org leadership panel",
        bar: "Multi-product architecture and long-range direction.",
      },
    ],
    loop: [
      {
        n: 1,
        name: "Online assessment",
        dur: "60–90 min",
        fmt: "HackerRank",
        tests: "Two problems, Medium.",
        levels: ["SDE", "SDE2"],
      },
      {
        n: 2,
        name: "Code design × 2",
        dur: "60 min each",
        fmt: "Live coding in your IDE",
        tests:
          "A small real-world problem to model and implement, then an extension that tests whether your model absorbs change. Data-structure choice is explicitly graded.",
        levels: ["SDE", "SDE2", "Lead SDE"],
      },
      {
        n: 3,
        name: "System design",
        dur: "60 min",
        fmt: "Whiteboard",
        tests:
          "Often an Atlassian-shaped product problem (tagging, search, notifications across Jira/Confluence). REST endpoint design is emphasised.",
        levels: ["SDE2", "Lead SDE", "Staff SDE", "Principal SDE"],
      },
      {
        n: 4,
        name: "Hiring manager",
        dur: "45 min",
        fmt: "Conversation",
        tests: "Successes, failures, conflict, leading vs supporting.",
        levels: ["SDE", "SDE2", "Lead SDE", "Staff SDE", "Principal SDE"],
      },
      {
        n: 5,
        name: "Values round",
        dur: "45 min",
        fmt: "Conversation",
        tests:
          "Atlassian's five values with a concrete example for each. This round genuinely rejects strong engineers.",
        levels: ["SDE", "SDE2", "Lead SDE", "Staff SDE", "Principal SDE"],
      },
    ],
    bank: [
      {
        q: "Design a file system that can return the size of any directory",
        t: "lld",
        lv: ["SDE", "SDE2", "Lead SDE"],
        d: "M",
        round: 2,
        ans: "lld-filemanager",
        note: "Reported verbatim in a senior loop. The extension — report the top N directories by size — is where the round is decided: keep sizes incrementally on write (each node caches its subtree size, updated up the parent chain) rather than recomputing, then a heap over nodes for top-N.",
      },
      {
        q: "Design a snake game",
        t: "lld",
        lv: ["SDE", "SDE2"],
        d: "M",
        lc: 353,
        slug: "design-snake-game",
        round: 2,
        a: "**The insight the interviewer is waiting for**: a move is *add a head, remove a tail* — a deque plus a set of occupied cells, so every move is O(1). Candidates who re-scan the body per move get marked down.\n\n**Model**: `Board` (width, height, food positions), `Snake` (`Deque<Cell> body`, `Set<Cell> occupied`, direction), `Game` (state machine RUNNING → GAME_OVER, score, tick). `move(direction)` computes the new head, checks wall collision, adds it, and — unless food was eaten — removes the tail **before** the self-collision check (the tail cell is about to be free, a classic off-by-one).\n\n**Extensions they add**: growing food, obstacles, multiple snakes (now collisions are pairwise and you need a shared occupancy map), and replay (store the move list).",
        code: {
          java: "class SnakeGame {\n    private final Deque<int[]> body = new ArrayDeque<>();        // head first\n    private final Set<Integer> occupied = new HashSet<>();       // encoded r*cols+c\n    private final int rows, cols;\n    private final Deque<int[]> food;\n    private int score = 0;\n\n    public int move(String direction) {\n        int[] head = body.peekFirst();\n        int[] next = step(head, direction);\n\n        if (next[0] < 0 || next[1] < 0 || next[0] >= rows || next[1] >= cols) return -1;  // wall\n\n        boolean ate = !food.isEmpty() && Arrays.equals(food.peek(), next);\n        if (!ate) {                                   // free the tail BEFORE self check\n            int[] tail = body.pollLast();\n            occupied.remove(encode(tail));\n        } else {\n            food.poll();\n            score++;\n        }\n        if (!occupied.add(encode(next))) return -1;   // self collision\n        body.addFirst(next);\n        return score;\n    }\n    private int encode(int[] c) { return c[0] * cols + c[1]; }\n}",
          py: "from collections import deque\n\nclass SnakeGame:\n    def __init__(self, width, height, food):\n        self.rows, self.cols = height, width\n        self.body = deque([(0, 0)])\n        self.occupied = {(0, 0)}\n        self.food = deque(food)\n        self.score = 0\n\n    def move(self, direction):\n        dr, dc = {'U': (-1, 0), 'D': (1, 0), 'L': (0, -1), 'R': (0, 1)}[direction]\n        r, c = self.body[0]\n        nxt = (r + dr, c + dc)\n        if not (0 <= nxt[0] < self.rows and 0 <= nxt[1] < self.cols):\n            return -1\n        ate = self.food and list(nxt) == list(self.food[0])\n        if not ate:\n            self.occupied.discard(self.body.pop())      # tail frees first\n        else:\n            self.food.popleft(); self.score += 1\n        if nxt in self.occupied:\n            return -1\n        self.occupied.add(nxt); self.body.appendleft(nxt)\n        return self.score",
        },
      },
      {
        q: "Design a tag management system across Jira/Confluence (add, remove, search)",
        t: "hld",
        lv: ["SDE2", "Lead SDE", "Staff SDE"],
        d: "M",
        round: 3,
        a: "**Clarify first** (they watch for this): scope of a tag — global, per site, per product? Who can create tags? Case sensitivity and localisation? Search semantics — prefix, fuzzy, multi-tag AND/OR? Scale — hundreds of millions of taggable entities across products.\n\n**API design is explicitly graded**, so write it out: `POST /v1/tags {name}`, `PUT /v1/entities/{type}/{id}/tags {tagIds[]}`, `DELETE /v1/entities/{type}/{id}/tags/{tagId}`, `GET /v1/tags?prefix=&siteId=&limit=&cursor=`, `GET /v1/entities?tag=a&tag=b&op=AND&cursor=`. Cursor pagination, idempotent PUT, and 409 on duplicate tag names per scope.\n\n**Data model** — `tags(tag_id, site_id, name UNIQUE(site_id, lower(name)), created_by)` and `entity_tags(site_id, entity_type, entity_id, tag_id, PRIMARY KEY(site_id, entity_type, entity_id, tag_id))` plus the inverse index `(site_id, tag_id, entity_type, entity_id)` — you need both directions: tags of an entity, and entities with a tag.\n\n**Search** — Elasticsearch for prefix/fuzzy tag lookup and for multi-tag filtering combined with permissions; the relational tables stay the source of truth and the index is rebuilt from them. **Permissions must be applied in the query**, not after, or you leak titles across a multi-tenant boundary.\n\n**Scale details**: hot tags (thousands of entities) need pagination, not full lists; tag rename is a metadata-only change if entities reference `tag_id` rather than the string — say that, it is the design payoff.",
        fu: [
          {
            q: "How do you show tag counts?",
            a: "Denormalised counters updated asynchronously from the tagging event stream, reconciled nightly. Counting on read across a billion-row join is the anti-pattern.",
          },
          {
            q: "Multi-product consistency?",
            a: "One tag service with product-scoped namespaces, exposed over an API rather than a shared table — otherwise every product schema change becomes a cross-team migration.",
          },
        ],
      },
      {
        q: "Rate limiter design",
        t: "hld",
        lv: ["SDE2", "Lead SDE"],
        d: "M",
        round: 3,
        ans: "hld-ratelimiter",
      },
      {
        q: "Tell me about a time you failed, and what changed afterwards",
        t: "behavioral",
        lv: ["SDE", "SDE2", "Lead SDE", "Staff SDE"],
        round: 4,
        ans: "b-challenging",
      },
      {
        q: "Give an example for each of our values (values round)",
        t: "behavioral",
        lv: ["SDE", "SDE2", "Lead SDE", "Staff SDE", "Principal SDE"],
        round: 5,
        a: 'Atlassian publishes five values and the round maps to them one by one. Prepare one specific story per value, each 90 seconds, each with a result:\n\n- **Open company, no bullshit** — a time you shared bad news early, or wrote the honest post-incident review.\n- **Build with heart and balance** — a time you protected quality or people against a deadline, and what you traded.\n- **Don\'t #@!% the customer** — a decision where you took the harder engineering path because the easy one would have hurt users.\n- **Play, as a team** — credit shared, or a time you picked up unglamorous work so the team could ship.\n- **Be the change you seek** — something you improved without being asked, with the before/after number.\n\nInterviewers probe for specifics: the date, the people, what you personally said. Generic "I always value transparency" answers fail this round.',
      },
    ],
    prep: [
      "Solve the base problem fast, then ask for the extension — the offer bar is base + extension in both code design rounds.",
      "Code in your own IDE with tests; they care about readable, extensible code more than clever algorithms.",
      "Write REST endpoints explicitly in the design round — it is called out as an evaluation dimension.",
      "The values round needs five prepared stories; treat it as seriously as the technical rounds.",
    ],
    sources: [
      {
        label: "GeeksforGeeks — Atlassian senior SWE experience",
        url: "https://www.geeksforgeeks.org/interview-experiences/atlassian-interview-experience-for-senior-software-engineer/",
      },
      {
        label: "CodingKaro — Atlassian experiences (43+)",
        url: "https://www.codingkaro.in/jobs-internships/leetcode-interview-experience/Atlassian",
      },
      {
        label: "EngineBogie — Atlassian senior SWE",
        url: "https://enginebogie.com/interview/experience/atlassian-senior-software-engineer/96",
      },
    ],
  },
  {
    id: "salesforce",
    name: "Salesforce",
    sector: "product",
    tag: "Multi-tenancy is the house specialty; practical design over competitive programming.",
    loc: "Hyderabad · Bengaluru",
    ladder: [
      {
        lvl: "SDE",
        internal: "MTS",
        yoe: "0–3",
        loop: "Screen + 2–3 technical",
        bar: "Solid Medium DSA, OOP, SQL.",
      },
      {
        lvl: "SDE2",
        internal: "SMTS-track",
        yoe: "3–6",
        loop: "HM + 2–3 technical + panel",
        bar: "Design a component, debug/extend existing code, API thinking.",
      },
      {
        lvl: "Lead SDE",
        internal: "SMTS (Senior)",
        yoe: "6–10",
        loop: "HM + coding + 1-hour system design + panel",
        bar: "Multi-tenant design, rate limiting, data partitioning, trade-off articulation.",
      },
      {
        lvl: "Staff SDE",
        internal: "LMTS / Principal",
        yoe: "10–14",
        loop: "Two design rounds + architecture panel",
        bar: "Platform-level design across orgs, governor limits and noisy-neighbour isolation.",
      },
      {
        lvl: "Principal SDE",
        internal: "PMTS / Architect",
        yoe: "14+",
        loop: "Architecture review board style panel",
        bar: "Sets architecture for a cloud/product line.",
      },
    ],
    loop: [
      {
        n: 1,
        name: "Recruiter screen",
        dur: "20–30 min",
        fmt: "Call",
        tests: "Fit and logistics.",
        levels: ["SDE", "SDE2", "Lead SDE", "Staff SDE", "Principal SDE"],
      },
      {
        n: 2,
        name: "Hiring manager round",
        dur: "45–60 min",
        fmt: "Conversation",
        tests: "Comes early at Salesforce: projects, ownership, why this team.",
        levels: ["SDE", "SDE2", "Lead SDE", "Staff SDE", "Principal SDE"],
      },
      {
        n: 3,
        name: "Technical × 1–3",
        dur: "45–60 min",
        fmt: "Live coding",
        tests:
          "LeetCode-Medium level algorithms, plus debugging or extending a provided snippet in Java/Python/JS.",
        levels: ["SDE", "SDE2", "Lead SDE", "Staff SDE"],
      },
      {
        n: 4,
        name: "System design",
        dur: "60 min",
        fmt: "Whiteboard",
        tests: "Multi-tenant SaaS problems: partitioning, API rate limits, async jobs.",
        levels: ["SDE2", "Lead SDE", "Staff SDE", "Principal SDE"],
      },
      {
        n: 5,
        name: "Panel / final",
        dur: "2–4 short rounds",
        fmt: "Mixed",
        tests: "Cross-functional peers, values (Trust, Customer Success, Innovation, Equality).",
        levels: ["SDE", "SDE2", "Lead SDE", "Staff SDE", "Principal SDE"],
      },
    ],
    bank: [
      {
        q: "Design a multi-tenant data partitioning scheme for a SaaS product with 100K+ tenants",
        t: "hld",
        lv: ["Lead SDE", "Staff SDE", "Principal SDE"],
        d: "H",
        round: 4,
        a: "**Clarify**: tenant size distribution (a few enterprises with millions of rows alongside 90K small tenants — this skew drives everything), isolation requirements (regulatory? separate encryption keys?), and whether cross-tenant analytics is needed.\n\n**Three models, and the answer is usually a hybrid**:\n1. **Shared schema, tenant_id column** — cheapest, densest, best for the long tail. Every table carries `tenant_id` as the leading column of every index and the partition key; a row-level security policy or a mandatory query filter prevents leaks.\n2. **Schema per tenant** — better isolation and per-tenant migrations, but thousands of schemas make DDL and connection pooling painful.\n3. **Database (or cluster) per tenant** — for the largest or most regulated customers; noisy-neighbour isolation is total and the cost per tenant is high.\n\n**Hybrid**: pooled shards for the long tail, dedicated shards for whales, with a `tenant → shard` routing table (not modulo, so a tenant can be migrated). Migration is dual-write, backfill, verify, flip, exactly as in the sharding answer in the library.\n\n**Noisy neighbour** is the Salesforce-specific dimension: per-tenant quotas (governor limits), per-tenant concurrency caps, fair-share queues for async jobs, and per-tenant metrics so one customer's batch job cannot consume a shard.\n\n**Safety**: never rely on the application remembering the filter — enforce `tenant_id` in a repository layer or with Postgres row-level security, and test cross-tenant access as a security test.",
        fu: [
          {
            q: "How do you run a schema migration across 100K tenants?",
            a: "Expand/contract only, applied online: add nullable column → backfill in batches by tenant → dual-write → switch reads → drop later. Track per-tenant migration state so a failure resumes rather than restarts.",
          },
          {
            q: "How do you support per-tenant customisation (custom fields)?",
            a: "Either JSONB columns with GIN indexes or an EAV side table; Salesforce's own answer historically was a wide generic table with per-tenant metadata mapping — mention the trade-off with query performance.",
          },
        ],
      },
      {
        q: "How would you design rate limiting for a public API serving millions of requests a day?",
        t: "hld",
        lv: ["SDE2", "Lead SDE", "Staff SDE"],
        d: "M",
        round: 4,
        ans: "hld-ratelimiter",
        note: "Salesforce's twist: limits are per tenant and per API version, and exceeding them must return a documented error that customers can code against.",
      },
      {
        q: "Debug and extend this code snippet",
        t: "coding",
        lv: ["SDE", "SDE2", "Lead SDE"],
        d: "M",
        round: 3,
        a: "A Salesforce-specific format: you are handed working-ish code and asked to find the bug, then add a feature. The method that scores well: **read the tests/inputs first**, state what the code claims to do, then find the mismatch — off-by-one, mutation during iteration, unclosed resource, wrong equality (`==` on boxed types), missing null case, or an unsynchronised shared field.\n\nNarrate as you go and add a failing test before fixing. For the extension, say explicitly which existing method you are changing and why the change is safe (no behaviour change for existing callers), then implement it.\n\nWhat they are testing is not cleverness — it is whether you can work in someone else's code without breaking it.",
      },
      {
        q: "Graph traversal / DP at Medium difficulty",
        t: "coding",
        lv: ["SDE", "SDE2", "Lead SDE"],
        d: "M",
        lc: 200,
        slug: "number-of-islands",
        round: 3,
        ans: "islands",
        note: "Salesforce explicitly does not go to competitive-programming Hard; Medium done cleanly is the bar.",
      },
      {
        q: "LRU cache",
        t: "coding",
        lv: ["SDE", "SDE2"],
        d: "M",
        lc: 146,
        slug: "lru-cache",
        round: 3,
        ans: "lru",
      },
      {
        q: "Tell me about a time you put the customer first",
        t: "behavioral",
        lv: ["SDE", "SDE2", "Lead SDE", "Staff SDE"],
        round: 5,
        a: "Map to their values: Trust, Customer Success, Innovation, Equality. The strongest version of this story has a moment where the engineering-convenient choice and the customer-right choice diverged, and you picked the customer with evidence (a support ticket trend, a churn risk, a latency number a customer actually felt).\n\nInclude what it cost — a slipped date, extra work, a harder migration — because the value only means something when it traded against something.",
      },
    ],
    prep: [
      "Know multi-tenancy cold: tenant isolation, per-tenant limits, noisy neighbours, and per-tenant migrations.",
      "Expect a debugging/extension exercise rather than only clean-sheet coding.",
      "The hiring manager round comes early and carries weight — have your project narrative ready before the technical rounds.",
    ],
    sources: [
      {
        label: "FinalRound — Salesforce interview process",
        url: "https://www.finalroundai.com/blog/salesforce-interview-process",
      },
      {
        label: "interviewing.io — Salesforce interview questions",
        url: "https://interviewing.io/salesforce-interview-questions",
      },
      {
        label: "Glassdoor — Salesforce senior SWE interviews",
        url: "https://www.glassdoor.co.in/Interview/Salesforce-Senior-Software-Engineer-Interview-Questions-EI_IE11159.0,10_KO11,35.htm",
      },
    ],
  },
  {
    id: "adobe",
    name: "Adobe",
    sector: "product",
    tag: "Strong CS fundamentals plus practical OOD; less system design than peers below senior.",
    loc: "Noida · Bengaluru",
    ladder: [
      {
        lvl: "SDE",
        internal: "MTS-1",
        yoe: "0–3",
        loop: "OA + 2–3 technical + HM",
        bar: "DSA (arrays, strings, trees), OOP, OS/DBMS basics.",
      },
      {
        lvl: "SDE2",
        internal: "MTS-2",
        yoe: "3–6",
        loop: "3 technical + design + HM",
        bar: "Medium/Hard DSA plus an LLD; language depth (Java/C++) is probed.",
      },
      {
        lvl: "Lead SDE",
        internal: "Computer Scientist",
        yoe: "6–10",
        loop: "Coding + LLD + HLD + HM + director",
        bar: "Owns a feature area; design round covers scale and media/asset pipelines.",
      },
      {
        lvl: "Staff SDE",
        internal: "Sr. Computer Scientist",
        yoe: "10–14",
        loop: "Design ×2 + architecture + leadership",
        bar: "Architecture across products, performance engineering depth.",
      },
      {
        lvl: "Principal SDE",
        internal: "Principal Scientist",
        yoe: "14+",
        loop: "Architecture panel",
        bar: "Direction for a product line, research-to-product translation.",
      },
    ],
    loop: [
      {
        n: 1,
        name: "Online assessment",
        dur: "60–90 min",
        fmt: "HackerRank",
        tests: "2–3 problems plus aptitude/CS MCQs for junior levels.",
        levels: ["SDE", "SDE2"],
      },
      {
        n: 2,
        name: "Technical × 2–3",
        dur: "45–60 min",
        fmt: "Live coding",
        tests:
          "DSA with a strong bias to arrays, strings, trees and DP; plus language internals and CS fundamentals.",
        levels: ["SDE", "SDE2", "Lead SDE"],
      },
      {
        n: 3,
        name: "Design",
        dur: "60 min",
        fmt: "Whiteboard",
        tests:
          "LLD at SDE2 (design a text editor, an image pipeline, a plugin system), HLD at senior (asset storage, sync, collaboration).",
        levels: ["SDE2", "Lead SDE", "Staff SDE", "Principal SDE"],
      },
      {
        n: 4,
        name: "Hiring manager / director",
        dur: "45 min",
        fmt: "Conversation",
        tests: "Project deep dive, ownership, collaboration.",
        levels: ["SDE", "SDE2", "Lead SDE", "Staff SDE", "Principal SDE"],
      },
    ],
    bank: [
      {
        q: "Design a text editor with undo/redo",
        t: "lld",
        lv: ["SDE2", "Lead SDE"],
        d: "M",
        round: 3,
        a: "**Clarify**: plain text or rich text, file size (a 1 GB file rules out a plain string), collaborative or single user, and how granular undo should be (per keystroke or per word/command).\n\n**Buffer choice is the real question**: a plain string is O(n) per insert; a **gap buffer** gives O(1) edits at the cursor (what Emacs uses); a **piece table** keeps the original immutable and a list of spans, making undo nearly free and loading huge files instant (what VS Code uses); a **rope** gives O(log n) inserts anywhere. Name at least two and justify one.\n\n**Undo/redo** is the Command pattern: every edit is a `Command` with `execute()`/`undo()` and enough state to invert itself; two stacks, and a new edit clears the redo stack. Coalesce consecutive keystrokes into one command by time/word boundary so undo does not feel broken.\n\n**Extensions they add**: find/replace (Boyer-Moore or the index you already keep), syntax highlighting (incremental re-lex of the changed range only), and collaborative editing (now you need OT or CRDTs — say which and why).",
        fu: [
          {
            q: "How would you make undo work across a collaborative session?",
            a: "Undo becomes per-user and must not revert someone else's edit: with CRDTs you invert only your own operations; with OT you transform the inverse against concurrent operations. Say that naive global undo is wrong here.",
          },
          {
            q: "Memory for a 2 GB file?",
            a: "Memory-map the file and use a piece table over it, so you never materialise the whole document; render only the visible viewport.",
          },
        ],
      },
      {
        q: "Longest Increasing Subsequence",
        t: "coding",
        lv: ["SDE2", "Lead SDE"],
        d: "M",
        lc: 300,
        slug: "longest-increasing-subsequence",
        round: 2,
        ans: "lis",
      },
      {
        q: "Trapping Rain Water",
        t: "coding",
        lv: ["SDE2", "Lead SDE"],
        d: "H",
        lc: 42,
        slug: "trapping-rain-water",
        round: 2,
        a: "**Brute force** — for each index, scan both ways for the max: O(n²).\n\n**Prefix arrays** — precompute `leftMax[]` and `rightMax[]`, then water at i is `min(leftMax[i], rightMax[i]) - height[i]`: O(n) time, O(n) space.\n\n**Two pointers, O(1) space** — move the side with the smaller current max inward; that side's water is determined because the other side guarantees a wall at least as tall. This is the version they want, and the justification is the whole answer.\n\n**Monotonic stack** alternative fills water layer by layer and is worth naming since it connects to the nearest-smaller-element family.",
        code: {
          java: "int trap(int[] h) {\n    int lo = 0, hi = h.length - 1;\n    int leftMax = 0, rightMax = 0, water = 0;\n    while (lo < hi) {\n        if (h[lo] < h[hi]) {                 // left side is the limiting wall\n            leftMax = Math.max(leftMax, h[lo]);\n            water += leftMax - h[lo];\n            lo++;\n        } else {\n            rightMax = Math.max(rightMax, h[hi]);\n            water += rightMax - h[hi];\n            hi--;\n        }\n    }\n    return water;\n}",
          py: "def trap(h):\n    lo, hi = 0, len(h) - 1\n    left_max = right_max = water = 0\n    while lo < hi:\n        if h[lo] < h[hi]:\n            left_max = max(left_max, h[lo])\n            water += left_max - h[lo]\n            lo += 1\n        else:\n            right_max = max(right_max, h[hi])\n            water += right_max - h[hi]\n            hi -= 1\n    return water",
        },
      },
      {
        q: "Java/C++ internals: memory model, garbage collection, virtual functions",
        t: "concept",
        lv: ["SDE", "SDE2", "Lead SDE"],
        round: 2,
        ans: "c-gc",
        note: "Adobe probes language internals harder than most product companies — for C++ roles expect vtables, RAII, move semantics and smart pointers instead of GC.",
      },
      {
        q: "Design an asset storage and sync service (Creative Cloud-like)",
        t: "hld",
        lv: ["Lead SDE", "Staff SDE"],
        d: "H",
        round: 3,
        ans: "hld-upload",
        note: "Adobe's angle adds sync: per-device state, conflict resolution (last-writer-wins with version vectors vs explicit conflict copies), and delta sync for large binary assets.",
      },
      {
        q: "Design an image processing pipeline",
        t: "hld",
        lv: ["SDE2", "Lead SDE", "Staff SDE"],
        d: "M",
        round: 3,
        a: "**Clarify**: synchronous (user waits for a thumbnail) or asynchronous, input sizes, transformations (resize, crop, format convert, filters), and whether results must be reproducible.\n\n**Shape**: upload straight to object storage with a pre-signed URL → an event lands on a queue → a pool of stateless workers pulls jobs → each derivative is written back keyed by `(asset_id, transform_hash)` → a CDN serves them. Making the output key a hash of the transform parameters gives you idempotency and caching for free: the same request never re-renders.\n\n**Scaling details**: separate queues per priority (interactive thumbnails vs batch re-encodes); autoscale workers on queue depth; cap per-job memory and time (image bombs are a real attack — a 50,000×50,000 PNG decompresses to gigabytes, so validate dimensions before decoding); and do the expensive formats (video, RAW) on dedicated hardware.\n\n**On-the-fly vs precompute**: precompute the 3–4 sizes you know you need, generate the long tail lazily on first request and cache at the CDN. Say the trade: precomputing everything wastes storage, generating everything lazily punishes the first viewer.",
      },
    ],
    prep: [
      "Fundamentals matter here: OS, DBMS, and language internals come up alongside DSA.",
      "For C++ teams, expect memory management, RAII and move semantics rather than JVM questions.",
      "LLD questions are often editor/document/plugin shaped — practise Command pattern and buffer data structures.",
    ],
    sources: [
      {
        label: "InterviewQuery — Adobe SWE guide",
        url: "https://www.interviewquery.com/interview-guides/adobe-software-engineer",
      },
      {
        label: "Glassdoor — Adobe senior SWE interviews",
        url: "https://www.glassdoor.co.in/Interview/Adobe-Senior-Software-Engineer-Interview-Questions-EI_IE1090.0,5_KO6,30.htm",
      },
    ],
  },
  {
    id: "oracle",
    name: "Oracle",
    sector: "product",
    tag: "Conversational depth on cloud infra and databases; coding is solid but not exotic.",
    loc: "Bengaluru · Hyderabad · Noida",
    ladder: [
      {
        lvl: "SDE",
        internal: "IC1/IC2",
        yoe: "0–3",
        loop: "Screen + 2–3 technical",
        bar: "DSA, SQL, OS basics.",
      },
      {
        lvl: "SDE2",
        internal: "IC3",
        yoe: "3–7",
        loop: "Phone screen + 4 onsite",
        bar: "Coding + one design + SQL depth; debugging conversations.",
      },
      {
        lvl: "Lead SDE",
        internal: "IC4 (Senior)",
        yoe: "7–11",
        loop: "Coding + design + technical depth + HM + team match",
        bar: "Distributed design, multi-tenant cloud concerns, production debugging stories.",
      },
      {
        lvl: "Staff SDE",
        internal: "IC5 (Consulting)",
        yoe: "11–15",
        loop: "Design ×2 + architecture + leadership",
        bar: "Service ownership at OCI scale, migration and compatibility strategy.",
      },
      {
        lvl: "Principal SDE",
        internal: "IC6 / Architect",
        yoe: "15+",
        loop: "Architecture panel",
        bar: "Cross-service architecture, standards and long-lived APIs.",
      },
    ],
    loop: [
      {
        n: 1,
        name: "Recruiter / HM screen",
        dur: "30 min",
        fmt: "Call",
        tests: "Background and fundamentals.",
        levels: ["SDE", "SDE2", "Lead SDE", "Staff SDE", "Principal SDE"],
      },
      {
        n: 2,
        name: "Technical phone screen",
        dur: "45 min",
        fmt: "Live coding",
        tests: "Basic DSA plus networking/OS fundamentals — deliberately foundational.",
        levels: ["SDE", "SDE2", "Lead SDE"],
      },
      {
        n: 3,
        name: "Onsite loop × 4",
        dur: "45–60 min each",
        fmt: "Mixed",
        tests:
          "Coding, SQL, system design, and a conversational technical-depth round on cloud, multi-tenancy and debugging.",
        levels: ["SDE", "SDE2", "Lead SDE", "Staff SDE"],
      },
      {
        n: 4,
        name: "Behavioural + team match",
        dur: "30–45 min",
        fmt: "Conversation",
        tests: "Project fit and org matching; varies a lot by team.",
        levels: ["SDE", "SDE2", "Lead SDE", "Staff SDE", "Principal SDE"],
      },
    ],
    bank: [
      {
        q: "Design a scalable rate limiter",
        t: "hld",
        lv: ["SDE2", "Lead SDE", "Staff SDE"],
        d: "M",
        round: 3,
        ans: "hld-ratelimiter",
      },
      {
        q: "Design a hit counter (requests in the last 5 minutes)",
        t: "lld",
        lv: ["SDE", "SDE2", "Lead SDE"],
        d: "M",
        lc: 362,
        slug: "design-hit-counter",
        round: 3,
        a: "**Naive** — store every timestamp in a list and count those within the window: O(n) memory for a high-QPS service, which is the failure they are looking for.\n\n**Bucketed ring** — an array of 300 buckets (one per second for a 5-minute window), each holding `(timestampOfBucket, count)`. `hit(t)`: index `t % 300`; if the stored timestamp differs, the bucket is stale so reset it to 1, else increment. `getHits(t)`: sum buckets whose timestamp is within `t-300`. O(1) hit, O(window) read, and **fixed memory regardless of traffic** — that is the answer.\n\n**Follow-ups**: finer granularity (more buckets), distributed counting (per-node rings summed at read, or Redis with per-second keys and TTL), and concurrency (per-bucket atomic counters rather than a global lock).",
        code: {
          java: "class HitCounter {\n    private static final int WINDOW = 300;          // seconds\n    private final int[] times = new int[WINDOW];\n    private final int[] hits  = new int[WINDOW];\n\n    public synchronized void hit(int timestamp) {\n        int i = timestamp % WINDOW;\n        if (times[i] != timestamp) {                // stale bucket -> reset\n            times[i] = timestamp;\n            hits[i] = 1;\n        } else {\n            hits[i]++;\n        }\n    }\n\n    public synchronized int getHits(int timestamp) {\n        int total = 0;\n        for (int i = 0; i < WINDOW; i++)\n            if (timestamp - times[i] < WINDOW) total += hits[i];\n        return total;\n    }\n}",
          py: "class HitCounter:\n    def __init__(self, window=300):\n        self.window = window\n        self.times = [0] * window\n        self.hits = [0] * window\n\n    def hit(self, timestamp):\n        i = timestamp % self.window\n        if self.times[i] != timestamp:\n            self.times[i], self.hits[i] = timestamp, 1\n        else:\n            self.hits[i] += 1\n\n    def get_hits(self, timestamp):\n        return sum(h for t, h in zip(self.times, self.hits)\n                   if timestamp - t < self.window)",
        },
      },
      {
        q: "SQL: second highest salary, and per-department variants",
        t: "concept",
        lv: ["SDE", "SDE2", "Lead SDE"],
        round: 3,
        ans: "c-join",
        note: "Oracle asks real SQL, not SQL-flavoured pseudocode: window functions, correlated subqueries, and why one plan beats another.",
      },
      {
        q: "LRU cache",
        t: "coding",
        lv: ["SDE", "SDE2"],
        d: "M",
        lc: 146,
        slug: "lru-cache",
        round: 2,
        ans: "lru",
      },
      {
        q: "Validate a binary search tree",
        t: "coding",
        lv: ["SDE", "SDE2"],
        d: "M",
        lc: 98,
        slug: "validate-binary-search-tree",
        round: 2,
        a: 'The trap is checking only `left < node < right` locally — that passes for a tree that is globally invalid. Two correct approaches:\n\n**Bounds recursion** — carry `(min, max)` down: every node must lie strictly inside, and the bounds tighten as you descend. Use `Long` bounds (or nullable) so `Integer.MIN_VALUE` as a value does not break it.\n\n**In-order traversal** — an in-order walk of a BST is strictly increasing; keep the previous value and compare. Equivalent cost, and it generalises to "find the two swapped nodes" (LC 99).\n\nBoth O(n) time, O(h) space.',
        code: {
          java: "boolean isValidBST(TreeNode root) {\n    return check(root, null, null);\n}\nprivate boolean check(TreeNode n, Integer lo, Integer hi) {\n    if (n == null) return true;\n    if (lo != null && n.val <= lo) return false;\n    if (hi != null && n.val >= hi) return false;\n    return check(n.left, lo, n.val) && check(n.right, n.val, hi);\n}",
          py: "def is_valid_bst(root):\n    def check(n, lo=float('-inf'), hi=float('inf')):\n        if not n:\n            return True\n        if not (lo < n.val < hi):\n            return False\n        return check(n.left, lo, n.val) and check(n.right, n.val, hi)\n    return check(root)",
        },
      },
      {
        q: "How would you debug a production issue in a distributed service?",
        t: "concept",
        lv: ["SDE2", "Lead SDE", "Staff SDE"],
        round: 3,
        ans: "c-504",
      },
      {
        q: "Explain multi-tenancy and how you isolate tenants in a cloud service",
        t: "concept",
        lv: ["Lead SDE", "Staff SDE", "Principal SDE"],
        round: 3,
        a: "Three isolation axes, and a good answer covers all three: **data** (shared schema with tenant_id and enforced filters, schema per tenant, or database per tenant), **compute** (shared pools with per-tenant quotas and fair-share scheduling, versus dedicated instances for large or regulated tenants), and **security** (per-tenant encryption keys, separate credentials, audited cross-tenant access).\n\nThe operational half: per-tenant metrics and limits so a noisy neighbour is visible and containable, per-tenant migration state so upgrades are staged, and the ability to move a tenant between shards (which requires a routing table, not modulo hashing).\n\nOracle-specific angle: they will push on how you'd handle a tenant that is 1000× the median — the answer is a dedicated shard plus the same code path, not a special-cased fork of the service.",
      },
    ],
    prep: [
      "Expect real SQL — window functions, execution plans, and index choices.",
      "The 'technical depth' round is conversational: prepare to discuss cloud architecture, multi-tenancy and a production debugging story.",
      "Team matching matters; the loop varies by org (OCI is closer to a systems interview than applications teams).",
    ],
    sources: [
      {
        label: "InterviewQuery — Oracle SWE guide",
        url: "https://www.interviewquery.com/interview-guides/oracle-software-engineer",
      },
      {
        label: "Glassdoor — Oracle senior SWE interviews",
        url: "https://www.glassdoor.co.in/Interview/Oracle-Senior-Software-Engineer-Interview-Questions-EI_IE1737.0,6_KO7,31_IP4.htm",
      },
    ],
  },
];
