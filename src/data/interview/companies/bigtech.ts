// Imported from the Interview Prep Console (companies-bigtech.js).
import type { Company } from "../types";

export const bigtechCompanies: Company[] = [
  {
    id: "google",
    name: "Google",
    sector: "product",
    tag: "Algorithms-first loop with a hiring committee; system design only from L5.",
    loc: "Bengaluru · Hyderabad · Pune",
    ladder: [
      {
        lvl: "SDE",
        internal: "L3 / SWE II",
        yoe: "0–2",
        loop: "1 phone screen + 4 onsite (3 coding, 1 Googliness)",
        bar: "Correct, clean code on Medium problems; clear communication.",
      },
      {
        lvl: "SDE2",
        internal: "L4",
        yoe: "2–5",
        loop: "1 phone screen + 4 onsite (2–3 coding, 1 Googliness, occasional light design)",
        bar: "Medium/Hard coding, optimal complexity, edge cases unprompted. No dedicated system design round.",
      },
      {
        lvl: "Lead SDE",
        internal: "L5 (Senior)",
        yoe: "5–9",
        loop: "Phone screen + 5 onsite (2 coding, 1–2 system design, Googliness)",
        bar: "System design becomes a real gate; ownership of a project with cross-team impact.",
      },
      {
        lvl: "Staff SDE",
        internal: "L6 (Staff)",
        yoe: "9–13",
        loop: "2 system design, 1–2 coding, Googliness + leadership deep dive",
        bar: "Design ambiguous problems end to end, define interfaces across teams, technical influence without authority.",
      },
      {
        lvl: "Principal SDE",
        internal: "L7+",
        yoe: "13+",
        loop: "Design-heavy panel, org-level leadership rounds, committee + SVP review",
        bar: "Company-level technical strategy, multi-year direction, evidence of building other senior engineers.",
      },
    ],
    loop: [
      {
        n: 0,
        name: "Google Hiring Assessment + recruiter screen",
        dur: "30–45 min each",
        fmt: "Online + call",
        tests: "Work style questionnaire and role fit. Not a technical gate but it is scored.",
        levels: ["SDE", "SDE2"],
      },
      {
        n: 1,
        name: "Technical phone screen",
        dur: "45 min",
        fmt: "Google Docs / shared editor",
        tests:
          "One or two Medium problems, written in a plain doc with no autocomplete — practise that.",
        levels: ["SDE", "SDE2", "Lead SDE", "Staff SDE"],
      },
      {
        n: 2,
        name: "Coding onsite × 2–3",
        dur: "45 min each",
        fmt: "Whiteboard or Doc",
        tests:
          "Data structures and algorithms with optimal complexity, dry runs, edge cases. Graphs, heaps, DP and string processing dominate.",
        levels: ["SDE", "SDE2", "Lead SDE", "Staff SDE"],
      },
      {
        n: 3,
        name: "System design",
        dur: "45–60 min",
        fmt: "Whiteboard",
        tests:
          "From L5 up. Scale estimation, storage choice, sharding, consistency trade-offs. Two of these at L6.",
        levels: ["Lead SDE", "Staff SDE", "Principal SDE"],
      },
      {
        n: 4,
        name: "Googliness & leadership",
        dur: "45 min",
        fmt: "Conversation",
        tests:
          "Collaboration, ambiguity, user focus, disagreement handled well. Scored and genuinely rejects people.",
        levels: ["SDE", "SDE2", "Lead SDE", "Staff SDE", "Principal SDE"],
      },
      {
        n: 5,
        name: "Hiring committee + team match",
        dur: "—",
        fmt: "Packet review",
        tests:
          "A committee that never met you reads the written feedback, so interviewer notes matter more than rapport. Then team matching.",
        levels: ["SDE", "SDE2", "Lead SDE", "Staff SDE", "Principal SDE"],
      },
    ],
    bank: [
      {
        q: "Merge Intervals",
        t: "coding",
        lv: ["SDE", "SDE2"],
        d: "M",
        lc: 56,
        slug: "merge-intervals",
        round: 1,
        ans: "intervals",
        note: "Reported in recent L4 loops. Sort by start, then merge in one pass.",
      },
      {
        q: "Number of Islands, plus a streaming variant",
        t: "coding",
        lv: ["SDE", "SDE2", "Lead SDE"],
        d: "M",
        lc: 200,
        slug: "number-of-islands",
        round: 2,
        ans: "islands",
      },
      {
        q: "Word Ladder",
        t: "coding",
        lv: ["SDE2", "Lead SDE"],
        d: "H",
        lc: 127,
        slug: "word-ladder",
        round: 2,
        ans: "wordladder",
      },
      {
        q: "LRU Cache",
        t: "coding",
        lv: ["SDE", "SDE2", "Lead SDE"],
        d: "M",
        lc: 146,
        slug: "lru-cache",
        round: 1,
        ans: "lru",
      },
      {
        q: "Longest Substring Without Repeating Characters",
        t: "coding",
        lv: ["SDE", "SDE2"],
        d: "M",
        lc: 3,
        slug: "longest-substring-without-repeating-characters",
        round: 1,
        ans: "lswr",
      },
      {
        q: "Implement an HTTP route matcher (/users/:id style patterns with wildcards)",
        t: "coding",
        lv: ["SDE2", "Lead SDE"],
        d: "M",
        round: 2,
        a: "A Trie over path segments, not over characters. Each node holds static children plus one optional `:param` child and one `*` catch-all. Matching walks segments and prefers, in order: an exact static child, then the param child (binding the value), then the wildcard. Registration is O(segments); lookup is O(segments) and independent of the number of routes, which is the point — a linear scan over a regex list is what they want you to improve on.\n\n**Complexity** — insert and match O(k) for k path segments; memory O(total segments).\n\n**Edge cases to raise**: trailing slashes, case sensitivity, conflicting routes (`/users/me` vs `/users/:id` — static wins), and HTTP method as the first level of the tree.",
        code: {
          java: 'class RouteTrie {\n    private static class Node {\n        Map<String, Node> statics = new HashMap<>();\n        Node param; String paramName;\n        Node wildcard;\n        Handler handler;\n    }\n    private final Node root = new Node();\n\n    void add(String pattern, Handler h) {\n        Node cur = root;\n        for (String seg : split(pattern)) {\n            if (seg.startsWith(":")) {\n                if (cur.param == null) { cur.param = new Node(); cur.param.paramName = seg.substring(1); }\n                cur = cur.param;\n            } else if (seg.equals("*")) {\n                if (cur.wildcard == null) cur.wildcard = new Node();\n                cur = cur.wildcard;\n            } else {\n                cur = cur.statics.computeIfAbsent(seg, k -> new Node());\n            }\n        }\n        cur.handler = h;\n    }\n\n    Match match(String path) {\n        Map<String, String> params = new HashMap<>();\n        Node n = walk(root, split(path), 0, params);\n        return n == null || n.handler == null ? null : new Match(n.handler, params);\n    }\n\n    private Node walk(Node n, List<String> segs, int i, Map<String, String> params) {\n        if (i == segs.size()) return n;\n        String seg = segs.get(i);\n        Node stat = n.statics.get(seg);                     // 1. exact wins\n        if (stat != null) { Node r = walk(stat, segs, i + 1, params); if (r != null) return r; }\n        if (n.param != null) {                              // 2. then :param\n            params.put(n.param.paramName, seg);\n            Node r = walk(n.param, segs, i + 1, params);\n            if (r != null) return r;\n            params.remove(n.param.paramName);\n        }\n        return n.wildcard;                                  // 3. then catch-all\n    }\n}',
          py: "class Node:\n    __slots__ = ('statics', 'param', 'param_name', 'wildcard', 'handler')\n    def __init__(self):\n        self.statics, self.param, self.param_name = {}, None, None\n        self.wildcard, self.handler = None, None\n\nclass RouteTrie:\n    def __init__(self):\n        self.root = Node()\n\n    def add(self, pattern, handler):\n        cur = self.root\n        for seg in pattern.strip('/').split('/'):\n            if seg.startswith(':'):\n                if not cur.param:\n                    cur.param = Node(); cur.param.param_name = seg[1:]\n                cur = cur.param\n            elif seg == '*':\n                cur.wildcard = cur.wildcard or Node()\n                cur = cur.wildcard\n            else:\n                cur = cur.statics.setdefault(seg, Node())\n        cur.handler = handler\n\n    def match(self, path):\n        params = {}\n        segs = path.strip('/').split('/')\n\n        def walk(n, i):\n            if i == len(segs):\n                return n\n            seg = segs[i]\n            if seg in n.statics:\n                r = walk(n.statics[seg], i + 1)\n                if r: return r\n            if n.param:\n                params[n.param.param_name] = seg\n                r = walk(n.param, i + 1)\n                if r: return r\n                params.pop(n.param.param_name, None)\n            return n.wildcard\n\n        node = walk(self.root, 0)\n        return (node.handler, params) if node and node.handler else None",
        },
        fu: [
          {
            q: "How would you support method-specific routes?",
            a: "Make the method the first segment of the key, or store a map of method → handler on the terminal node. The second is better because it gives a proper 405 Method Not Allowed instead of a 404.",
          },
          {
            q: "Millions of routes?",
            a: "The Trie is already independent of route count for lookup. The memory optimisation is a radix tree that compresses single-child chains — which is what Gin, Echo and httprouter actually ship.",
          },
        ],
      },
      {
        q: "Design a distributed rate limiter for an internal API platform",
        t: "hld",
        lv: ["Lead SDE", "Staff SDE"],
        d: "M",
        round: 3,
        ans: "hld-ratelimiter",
      },
      {
        q: "Design a web crawler",
        t: "hld",
        lv: ["Lead SDE", "Staff SDE", "Principal SDE"],
        d: "H",
        round: 3,
        ans: "hld-crawler",
      },
      {
        q: "Design a log/metrics pipeline handling a petabyte a day",
        t: "hld",
        lv: ["Staff SDE", "Principal SDE"],
        d: "H",
        round: 3,
        ans: "hld-logging",
        note: "Google-flavoured follow-ups: sampling strategy, tail latency in the collection path, and how you would keep the schema stable across hundreds of teams.",
      },
      {
        q: "What is consistent hashing, and where would you use it here?",
        t: "concept",
        lv: ["SDE2", "Lead SDE", "Staff SDE"],
        round: 3,
        ans: "c-consistenthash",
      },
      {
        q: "Explain the CAP theorem and where your design sits",
        t: "concept",
        lv: ["Lead SDE", "Staff SDE"],
        round: 3,
        ans: "c-cap",
      },
      {
        q: "Tell me about a time you took initiative beyond your assigned work (Googliness)",
        t: "behavioral",
        lv: ["SDE", "SDE2", "Lead SDE", "Staff SDE", "Principal SDE"],
        round: 4,
        a: 'Googliness scores four things: **comfort with ambiguity**, **bias to act**, **collaboration without ego**, and **user focus**. Use a STAR story where you saw a gap nobody owned, checked it was worth doing (data, not instinct), acted with a small reversible step, and brought others along.\n\nA shape that works: *"Our on-call load was 12 pages a week and everyone treated it as normal. I pulled three months of pages, showed 60% came from one retry loop, wrote the fix and a dashboard, and got the rotation down to 3 pages a week. I did not own that service — I asked its owner to review and let them ship it."* Numbers, plus credit shared, plus a user-visible outcome.\n\nAvoid: heroics that bypassed the team, and any story where the initiative was really just "I worked weekends".',
      },
      {
        q: "How do you handle changing requirements mid-project?",
        t: "behavioral",
        lv: ["SDE2", "Lead SDE", "Staff SDE"],
        round: 4,
        a: 'Answer with a mechanism, not an attitude. The strong version: separate the *decision* from the *rework* — re-derive what changed in the requirement, state the cost of the change in concrete terms ("this adds two weeks and invalidates the sharding choice"), offer two options with trade-offs, and let the owner pick with that information. Then talk about how the design absorbed the change (interfaces, flags, staged rollout), because that is the engineering half of the answer.\n\nClose with what you changed afterwards so the same surprise costs less next time — a design doc review with the product owner earlier, or a thinner first milestone.',
      },
    ],
    prep: [
      "Practise writing code in a plain Google Doc — no autocomplete, no compiler. Candidates lose points to syntax slips they never make in an IDE.",
      "Google weights optimal complexity heavily: state the brute force, state its complexity, then optimise before writing.",
      "L5+ candidates should prepare two system designs end to end, with numbers, because the design round is where the level is decided.",
      "Googliness is a real gate — prepare four STAR stories with measurable results and shared credit.",
    ],
    sources: [
      {
        label: "Hello Interview — Google L4 guide",
        url: "https://www.hellointerview.com/guides/google/l4",
      },
      {
        label: "IGotAnOffer — Google L4 interview",
        url: "https://igotanoffer.com/en/advice/google-l4-interview",
      },
      {
        label: "InterviewPilot — Google SWE experiences 2025–26",
        url: "https://interviewpilot.dev/interview-experiences/google",
      },
    ],
  },
  {
    id: "amazon",
    name: "Amazon",
    sector: "product",
    tag: "Every round is half Leadership Principles; the bar raiser can veto the loop.",
    loc: "Bengaluru · Hyderabad · Chennai · Pune",
    ladder: [
      {
        lvl: "SDE",
        internal: "SDE I (L4)",
        yoe: "0–2",
        loop: "OA + 3–4 rounds",
        bar: "Coding correctness, basic OOD, 2 LP stories per round.",
      },
      {
        lvl: "SDE2",
        internal: "SDE II (L5)",
        yoe: "2–6",
        loop: "Phone screen + 5 onsite: 2–3 coding, 1–2 design, bar raiser",
        bar: "LLD and a scoped HLD; every technical answer must carry an LP story.",
      },
      {
        lvl: "Lead SDE",
        internal: "SDE III (L6)",
        yoe: "6–10",
        loop: "5–6 onsite: coding, LLD, HLD, bar raiser, hiring manager",
        bar: "Owns a service end to end; design round is the deciding round; Deliver Results and Ownership stories with numbers.",
      },
      {
        lvl: "Staff SDE",
        internal: "Principal-track / L7 SDE",
        yoe: "10–14",
        loop: "Design-heavy, multiple bar raisers, written narrative sometimes requested",
        bar: "Org-wide technical decisions, cost and operational excellence, mentoring at scale.",
      },
      {
        lvl: "Principal SDE",
        internal: "Principal (L7/L8)",
        yoe: "14+",
        loop: "Panel with directors/VPs, deep-dive on written docs",
        bar: "Sets technical direction for a business unit; invents and simplifies at company scale.",
      },
    ],
    loop: [
      {
        n: 1,
        name: "Online assessment",
        dur: "90–120 min",
        fmt: "HackerRank-style + work simulation",
        tests: "Two coding problems plus a work-style survey and a debugging section.",
        levels: ["SDE", "SDE2"],
      },
      {
        n: 2,
        name: "Technical phone screen",
        dur: "60 min",
        fmt: "Live coding",
        tests: "One or two coding problems plus system design fundamentals and LP probing.",
        levels: ["SDE", "SDE2", "Lead SDE"],
      },
      {
        n: 3,
        name: "Coding × 2–3",
        dur: "60 min each",
        fmt: "Live coding",
        tests: "Graphs, DP, concurrency patterns. Each round opens or closes with 2 LP questions.",
        levels: ["SDE", "SDE2", "Lead SDE", "Staff SDE"],
      },
      {
        n: 4,
        name: "Low-level / object-oriented design",
        dur: "60 min",
        fmt: "Coding + class design",
        tests: "Design an extensible component with clean interfaces; often becomes concurrency.",
        levels: ["SDE2", "Lead SDE", "Staff SDE"],
      },
      {
        n: 5,
        name: "System design",
        dur: "60 min",
        fmt: "Whiteboard",
        tests: "Scale, storage, failure modes, cost. Two of these from SDE III up.",
        levels: ["SDE2", "Lead SDE", "Staff SDE", "Principal SDE"],
      },
      {
        n: 6,
        name: "Bar raiser",
        dur: "60 min",
        fmt: "Mixed",
        tests:
          "An interviewer from another org with veto power. Deep LP probing plus one technical dimension.",
        levels: ["SDE", "SDE2", "Lead SDE", "Staff SDE", "Principal SDE"],
      },
    ],
    bank: [
      {
        q: "Implement a rate limiter using the token bucket algorithm",
        t: "lld",
        lv: ["SDE2", "Lead SDE", "Staff SDE"],
        d: "M",
        round: 4,
        ans: "lld-ratelimiter",
        note: "Reported in current SDE II loops as a coding round, not a design round — they want working code plus thread safety.",
      },
      {
        q: "Longest consecutive sequence in an unsorted array",
        t: "coding",
        lv: ["SDE", "SDE2"],
        d: "M",
        lc: 128,
        slug: "longest-consecutive-sequence",
        round: 3,
        a: "**Brute force** — sort, then walk counting runs: O(n log n) time, O(1) extra space if sorting in place.\n\n**Optimal** — put every value in a hash set. For each value, only start counting when `v-1` is absent (so each sequence is walked exactly once), then extend upward while `v+1` is present. Each element is visited at most twice, so it is O(n) time and O(n) space.\n\nThe interviewer is checking the `if (!set.contains(v-1))` guard — without it the inner loop makes it quadratic, and candidates often miss that the guard is what makes the amortised argument work.",
        code: {
          java: "int longestConsecutive(int[] nums) {\n    Set<Integer> set = new HashSet<>();\n    for (int v : nums) set.add(v);\n    int best = 0;\n    for (int v : set) {\n        if (set.contains(v - 1)) continue;     // only start at a sequence head\n        int len = 1, cur = v;\n        while (set.contains(cur + 1)) { cur++; len++; }\n        best = Math.max(best, len);\n    }\n    return best;\n}",
          py: "def longest_consecutive(nums):\n    s = set(nums)\n    best = 0\n    for v in s:\n        if v - 1 in s:\n            continue                  # not a sequence head\n        cur, length = v, 1\n        while cur + 1 in s:\n            cur += 1; length += 1\n        best = max(best, length)\n    return best",
        },
        fu: [
          {
            q: "Can you do it in O(1) space?",
            a: "Not without sorting; state the trade: sorting gives O(n log n) time and O(1) space, hashing gives O(n) time and O(n) space. Which you pick depends on whether memory or latency is scarce.",
          },
          {
            q: "Streaming version?",
            a: "Keep a map from endpoint value → run length and merge runs on insert (the classic union-by-boundary trick). Each insert is O(1) amortised.",
          },
        ],
      },
      {
        q: "Design a thread-safe circular buffer with concurrent read and write",
        t: "lld",
        lv: ["SDE2", "Lead SDE", "Staff SDE"],
        d: "M",
        round: 4,
        a: "Clarify first: single producer / single consumer, or many of each? Blocking or lock-free? Overwrite the oldest entry when full, or block the writer?\n\n**Design** — a fixed array plus head and tail indices. For the general multi-producer case use a `ReentrantLock` with two `Condition`s (`notFull`, `notEmpty`) — this is exactly how `ArrayBlockingQueue` works and saying so is a good signal. For the SPSC case you can go lock-free with two `volatile`/atomic counters and no lock at all, because only one thread mutates each index; that is the Disruptor's core insight.\n\n**Details that earn the round**: use `count` (or a sequence number) rather than comparing head==tail so full and empty are distinguishable; wake with `signal()` not `signalAll()` to avoid thundering herds; and use a power-of-two capacity so the modulo becomes a mask.",
        code: {
          java: 'public class CircularBuffer<T> {\n    private final Object[] items;\n    private int head = 0, tail = 0, count = 0;\n    private final ReentrantLock lock = new ReentrantLock();\n    private final Condition notFull  = lock.newCondition();\n    private final Condition notEmpty = lock.newCondition();\n\n    public CircularBuffer(int capacity) { items = new Object[capacity]; }\n\n    public void put(T item) throws InterruptedException {\n        lock.lock();\n        try {\n            while (count == items.length) notFull.await();   // while, never if\n            items[tail] = item;\n            tail = (tail + 1) % items.length;\n            count++;\n            notEmpty.signal();\n        } finally { lock.unlock(); }\n    }\n\n    @SuppressWarnings("unchecked")\n    public T take() throws InterruptedException {\n        lock.lock();\n        try {\n            while (count == 0) notEmpty.await();\n            T item = (T) items[head];\n            items[head] = null;                              // help GC\n            head = (head + 1) % items.length;\n            count--;\n            notFull.signal();\n            return item;\n        } finally { lock.unlock(); }\n    }\n}',
          py: "import threading\n\nclass CircularBuffer:\n    def __init__(self, capacity):\n        self.items = [None] * capacity\n        self.head = self.tail = self.count = 0\n        self.lock = threading.Lock()\n        self.not_full = threading.Condition(self.lock)\n        self.not_empty = threading.Condition(self.lock)\n\n    def put(self, item):\n        with self.not_full:\n            while self.count == len(self.items):\n                self.not_full.wait()\n            self.items[self.tail] = item\n            self.tail = (self.tail + 1) % len(self.items)\n            self.count += 1\n            self.not_empty.notify()\n\n    def take(self):\n        with self.not_empty:\n            while self.count == 0:\n                self.not_empty.wait()\n            item = self.items[self.head]\n            self.items[self.head] = None\n            self.head = (self.head + 1) % len(self.items)\n            self.count -= 1\n            self.not_full.notify()\n            return item",
        },
        fu: [
          {
            q: "Why `while` and not `if` around await?",
            a: "Spurious wakeups, and multi-consumer races where another thread takes the item between the signal and your reacquiring the lock. This is the single most common bug in this question.",
          },
          {
            q: "Make it lock-free.",
            a: "For single-producer/single-consumer, atomic head and tail with acquire/release semantics suffice — no CAS loop needed. For multi-producer, a CAS on the tail sequence plus a per-slot sequence number (LMAX Disruptor / Vyukov MPMC queue).",
          },
        ],
      },
      {
        q: "Minimum Window Substring",
        t: "coding",
        lv: ["SDE2", "Lead SDE"],
        d: "H",
        lc: 76,
        slug: "minimum-window-substring",
        round: 3,
        a: "**Brute force** — every substring, check coverage: O(n²·m).\n\n**Optimal sliding window** — expand `right` until the window covers all required characters, then contract `left` while it still covers, recording the best. Track `required` (distinct chars needed) and a `formed` counter so coverage is checked in O(1) rather than by comparing maps. O(n + m) time, O(m) space.\n\nThe part interviewers grade: the `formed` counter (comparing two hash maps per step is the naive version) and correct handling of duplicate characters in the target.",
        code: {
          java: 'String minWindow(String s, String t) {\n    if (s.length() < t.length()) return "";\n    Map<Character, Integer> need = new HashMap<>();\n    for (char c : t.toCharArray()) need.merge(c, 1, Integer::sum);\n\n    int required = need.size(), formed = 0;\n    Map<Character, Integer> win = new HashMap<>();\n    int bestLen = Integer.MAX_VALUE, bestL = 0, left = 0;\n\n    for (int right = 0; right < s.length(); right++) {\n        char c = s.charAt(right);\n        win.merge(c, 1, Integer::sum);\n        if (need.containsKey(c) && win.get(c).intValue() == need.get(c).intValue()) formed++;\n\n        while (formed == required) {\n            if (right - left + 1 < bestLen) { bestLen = right - left + 1; bestL = left; }\n            char l = s.charAt(left++);\n            win.merge(l, -1, Integer::sum);\n            if (need.containsKey(l) && win.get(l) < need.get(l)) formed--;\n        }\n    }\n    return bestLen == Integer.MAX_VALUE ? "" : s.substring(bestL, bestL + bestLen);\n}',
          py: "from collections import Counter\n\ndef min_window(s, t):\n    if len(s) < len(t):\n        return ''\n    need = Counter(t)\n    required, formed = len(need), 0\n    win = Counter()\n    best = (float('inf'), 0)\n    left = 0\n    for right, c in enumerate(s):\n        win[c] += 1\n        if c in need and win[c] == need[c]:\n            formed += 1\n        while formed == required:\n            if right - left + 1 < best[0]:\n                best = (right - left + 1, left)\n            l = s[left]; left += 1\n            win[l] -= 1\n            if l in need and win[l] < need[l]:\n                formed -= 1\n    return '' if best[0] == float('inf') else s[best[1]:best[1] + best[0]]",
        },
      },
      {
        q: "Serialize and deserialize an N-ary tree",
        t: "coding",
        lv: ["SDE2", "Lead SDE"],
        d: "H",
        lc: 428,
        slug: "serialize-and-deserialize-n-ary-tree",
        round: 3,
        a: "Encode each node as `value,childCount` in pre-order; deserialisation then reads a value, reads the child count, and recursively builds exactly that many children — no sentinel needed and no ambiguity. O(n) both ways.\n\nAlternatives worth naming: a bracketed format (`1[3[5 6] 2 4]`) which is human-readable but needs a parser, and level-order with null markers which is easier to stream. Say which you would pick and why: the child-count form is the most compact and the simplest to get right under time pressure.\n\nThe follow-up is usually about very deep trees (recursion → explicit stack) or very wide trees (streaming instead of building the whole string).",
        code: {
          java: "// \"1,3,3,2,5,0,6,0,2,0,4,0\"  ->  value,childCount pre-order\nString serialize(Node root) {\n    StringBuilder sb = new StringBuilder();\n    ser(root, sb);\n    return sb.toString();\n}\nprivate void ser(Node n, StringBuilder sb) {\n    if (n == null) return;\n    sb.append(n.val).append(',').append(n.children.size()).append(',');\n    for (Node c : n.children) ser(c, sb);\n}\n\nNode deserialize(String data) {\n    if (data.isEmpty()) return null;\n    Deque<String> q = new ArrayDeque<>(Arrays.asList(data.split(\",\")));\n    return des(q);\n}\nprivate Node des(Deque<String> q) {\n    if (q.isEmpty()) return null;\n    int val = Integer.parseInt(q.poll());\n    int count = Integer.parseInt(q.poll());\n    Node node = new Node(val, new ArrayList<>());\n    for (int i = 0; i < count; i++) node.children.add(des(q));\n    return node;\n}",
          py: "def serialize(root):\n    out = []\n    def ser(n):\n        if not n: return\n        out.append(str(n.val)); out.append(str(len(n.children)))\n        for c in n.children:\n            ser(c)\n    ser(root)\n    return ','.join(out)\n\ndef deserialize(data):\n    if not data:\n        return None\n    tokens = iter(data.split(','))\n    def des():\n        val = int(next(tokens)); count = int(next(tokens))\n        node = Node(val, [])\n        for _ in range(count):\n            node.children.append(des())\n        return node\n    return des()",
        },
      },
      {
        q: "Design a real-time stock price tracking system",
        t: "hld",
        lv: ["Lead SDE", "Staff SDE"],
        d: "H",
        round: 5,
        ans: "hld-stock",
        note: "Amazon's framing adds sub-100 ms latency and 99.99% uptime as stated NFRs, and expects WebSocket fan-out, Kafka and a time-series store by name.",
      },
      {
        q: "Design an e-commerce order and inventory system (never oversell)",
        t: "lld",
        lv: ["SDE2", "Lead SDE", "Staff SDE"],
        d: "H",
        round: 4,
        ans: "lld-ecommerce",
      },
      {
        q: "Design a document/object upload and processing pipeline (S3-like)",
        t: "hld",
        lv: ["Lead SDE", "Staff SDE"],
        d: "M",
        round: 5,
        ans: "hld-upload",
      },
      {
        q: "Ownership: describe a project you owned from inception to production",
        t: "behavioral",
        lv: ["SDE2", "Lead SDE", "Staff SDE", "Principal SDE"],
        round: 6,
        a: 'Amazon behavioural answers are scored against specific Leadership Principles, so name the principle implicitly through the story shape. Ownership wants: you defined the problem, you carried it past the fun part (launch → operations), and you still own the consequences.\n\nUse STAR with the **T** and **A** heavily weighted toward your individual actions — "we" without "I did X" is the most common reason a story gets marked down. Bring two numbers: the business metric and the engineering metric.\n\nEnd with the unglamorous part — the runbook, the alarms, the on-call handover, the cost after six months. That is what separates an Ownership story from a Deliver Results story.',
      },
      {
        q: "Dive Deep: walk me through debugging a production issue",
        t: "behavioral",
        lv: ["SDE", "SDE2", "Lead SDE", "Staff SDE"],
        round: 6,
        a: 'Structure: symptom → what the data said → hypothesis → how you tested it → root cause → fix → prevention. The principle being tested is whether you go to the lowest level of detail yourself rather than delegating the mystery.\n\nStrong answers quote actual evidence: a metric, a log line, a flame graph, a query plan. Weak answers stay at the narrative level ("we found a memory leak") without saying how.\n\nFinish with the second-order fix — the alarm that would have caught it 40 minutes earlier, or the load test that now runs in CI. Amazon asks "what did you change so it cannot happen again" almost every time.',
      },
      {
        q: "Earn Trust: a disagreement with a senior engineer",
        t: "behavioral",
        lv: ["SDE2", "Lead SDE", "Staff SDE", "Principal SDE"],
        round: 6,
        a: "Pick a disagreement that was resolved by evidence, not by rank or by giving up. The arc: you stated your position and the reason, you made the disagreement cheap to resolve (a prototype, a benchmark, a doc with options), you accepted the outcome, and the relationship survived.\n\nInclude a case where **you** were the one who turned out to be wrong — Have Backbone; Disagree and Commit is the paired principle, and a candidate who has only ever been proved right reads as either unlucky or untruthful.",
      },
    ],
    prep: [
      "Prepare 12–14 STAR stories mapped to the 16 Leadership Principles, each with numbers; you will be asked 2 per round and repeats across rounds are noticed.",
      "The bar raiser is from a different org and can veto — treat that round as the most important one, not the friendliest.",
      "Expect concurrency in the coding rounds (thread-safe structures, producer/consumer) more than at most companies.",
      "For SDE III+, operational excellence is a scored dimension: cost, alarms, runbooks and on-call all belong in your design answers.",
    ],
    sources: [
      {
        label: "Onsites.fyi — Amazon SDE II questions 2025",
        url: "https://www.onsites.fyi/blog/article/amazon-sde-ii-software-engineer-interview-questions",
      },
      {
        label: "Amazon SDE-3 interview experience, May 2025 (Medium)",
        url: "https://medium.com/@rohitverma_87831/amazon-sde-3-interview-experience-may-2025-where-engineering-meets-ownership-92b90d257bc8",
      },
      {
        label: "IGotAnOffer — Amazon SDE 2 interview",
        url: "https://igotanoffer.com/en/advice/amazon-sde-2-interview",
      },
    ],
  },
  {
    id: "microsoft",
    name: "Microsoft",
    sector: "product",
    tag: "Broad loop: DSA, LLD with real threading, HLD, and an 'as-appropriate' final round.",
    loc: "Bengaluru · Hyderabad · Noida",
    ladder: [
      {
        lvl: "SDE",
        internal: "SDE (L59/L60)",
        yoe: "0–3",
        loop: "OA + 3–4 rounds",
        bar: "Clean DSA, basic OOD, fundamentals (OS, DB).",
      },
      {
        lvl: "SDE2",
        internal: "SDE II (L61/L62)",
        yoe: "3–6",
        loop: "OA + 4 rounds incl. LLD and a light HLD",
        bar: "Working code for a component with concurrency; can defend design choices.",
      },
      {
        lvl: "Lead SDE",
        internal: "Senior SDE (L63/L64)",
        yoe: "6–10",
        loop: "4–5 rounds: DSA, LLD, HLD, hiring manager, 'as appropriate' round",
        bar: "Full HLD ownership, cross-team design, mentoring evidence.",
      },
      {
        lvl: "Staff SDE",
        internal: "Principal SDE (L65/L66)",
        yoe: "10–15",
        loop: "Design-heavy panel + skip-level",
        bar: "Architecture for a product area, technical strategy, hiring and levelling others.",
      },
      {
        lvl: "Principal SDE",
        internal: "Partner track (L67+)",
        yoe: "15+",
        loop: "Panel with partners/CVPs",
        bar: "Multi-org architecture and long-range platform bets.",
      },
    ],
    loop: [
      {
        n: 1,
        name: "Online assessment",
        dur: "60–90 min",
        fmt: "Codility / HackerRank",
        tests: "Two or three problems, typically Medium.",
        levels: ["SDE", "SDE2"],
      },
      {
        n: 2,
        name: "DSA round × 1–2",
        dur: "45–60 min",
        fmt: "Live coding",
        tests: "Trees, graphs, strings, stacks; dry run expected on the spot.",
        levels: ["SDE", "SDE2", "Lead SDE", "Staff SDE"],
      },
      {
        n: 3,
        name: "Low-level design",
        dur: "60 min",
        fmt: "Coding + design",
        tests:
          "Build a component with real code: cache, scheduler, key-value store with TTL, filesystem, rate limiter. Thread safety is nearly always probed.",
        levels: ["SDE2", "Lead SDE", "Staff SDE"],
      },
      {
        n: 4,
        name: "High-level design",
        dur: "60 min",
        fmt: "Whiteboard",
        tests: "Twitter feed, URL shortener, chat, load balancer. Scale and failure discussion.",
        levels: ["SDE2", "Lead SDE", "Staff SDE", "Principal SDE"],
      },
      {
        n: 5,
        name: "Hiring manager / 'as appropriate' round",
        dur: "45–60 min",
        fmt: "Mixed",
        tests:
          "The AA interviewer is senior and can override the loop. Projects, trade-offs, culture, plus one technical dimension of their choosing.",
        levels: ["SDE", "SDE2", "Lead SDE", "Staff SDE", "Principal SDE"],
      },
    ],
    bank: [
      {
        q: "Design an in-memory key-value store with expiry (TTL)",
        t: "lld",
        lv: ["SDE2", "Lead SDE", "Staff SDE"],
        d: "M",
        round: 3,
        ans: "lld-cache",
        note: "Microsoft's version adds TTL and asks for the eviction/expiry strategy explicitly — lazy expiry on read plus a sampling sweeper, and why a timing wheel beats a per-key timer.",
      },
      {
        q: "LRU Cache",
        t: "coding",
        lv: ["SDE", "SDE2", "Lead SDE"],
        d: "M",
        lc: 146,
        slug: "lru-cache",
        round: 2,
        ans: "lru",
      },
      {
        q: "Maximum Frequency Stack",
        t: "coding",
        lv: ["SDE2", "Lead SDE"],
        d: "H",
        lc: 895,
        slug: "maximum-frequency-stack",
        round: 2,
        a: "**Key insight** — keep a map from frequency → stack of values at that frequency, plus a map from value → its current frequency, plus `maxFreq`. Push: increment the value's frequency and append it to the stack for that frequency. Pop: take from `stacks[maxFreq]`, decrement, and drop `maxFreq` when its stack empties.\n\nBoth operations are O(1). The reason it works is that the per-frequency stack preserves insertion order *within* a frequency, which is exactly the tie-break the problem asks for (most recent wins).\n\nBrute force for contrast: scan the whole stack counting frequencies on every pop — O(n) per pop.",
        code: {
          java: "class FreqStack {\n    private final Map<Integer, Integer> freq = new HashMap<>();\n    private final Map<Integer, Deque<Integer>> group = new HashMap<>();\n    private int maxFreq = 0;\n\n    public void push(int val) {\n        int f = freq.merge(val, 1, Integer::sum);\n        maxFreq = Math.max(maxFreq, f);\n        group.computeIfAbsent(f, k -> new ArrayDeque<>()).push(val);\n    }\n\n    public int pop() {\n        Deque<Integer> top = group.get(maxFreq);\n        int val = top.pop();\n        freq.merge(val, -1, Integer::sum);\n        if (top.isEmpty()) maxFreq--;      // no gaps: frequencies decrease by 1\n        return val;\n    }\n}",
          py: "from collections import defaultdict\n\nclass FreqStack:\n    def __init__(self):\n        self.freq = defaultdict(int)\n        self.group = defaultdict(list)\n        self.max_freq = 0\n\n    def push(self, val):\n        self.freq[val] += 1\n        f = self.freq[val]\n        self.max_freq = max(self.max_freq, f)\n        self.group[f].append(val)\n\n    def pop(self):\n        val = self.group[self.max_freq].pop()\n        self.freq[val] -= 1\n        if not self.group[self.max_freq]:\n            self.max_freq -= 1\n        return val",
        },
      },
      {
        q: "Validate Stack Sequences",
        t: "coding",
        lv: ["SDE", "SDE2"],
        d: "M",
        lc: 946,
        slug: "validate-stack-sequences",
        round: 2,
        a: "Simulate: push from `pushed` one at a time, and after each push, while the stack top equals the next expected value in `popped`, pop and advance. If the stack is empty at the end, the sequence is valid. O(n) time, O(n) space.\n\nThe insight to say out loud: at any moment there is only one legal move — if the top matches the next pop, you *must* pop, otherwise you must push. That determinism is why a greedy simulation is provably correct here.",
        code: {
          java: "boolean validateStackSequences(int[] pushed, int[] popped) {\n    Deque<Integer> stack = new ArrayDeque<>();\n    int j = 0;\n    for (int v : pushed) {\n        stack.push(v);\n        while (!stack.isEmpty() && j < popped.length && stack.peek() == popped[j]) {\n            stack.pop();\n            j++;\n        }\n    }\n    return stack.isEmpty();\n}",
          py: "def validate_stack_sequences(pushed, popped):\n    stack, j = [], 0\n    for v in pushed:\n        stack.append(v)\n        while stack and j < len(popped) and stack[-1] == popped[j]:\n            stack.pop(); j += 1\n    return not stack",
        },
      },
      {
        q: "Binary Tree Zigzag Level Order Traversal",
        t: "coding",
        lv: ["SDE", "SDE2"],
        d: "M",
        lc: 103,
        slug: "binary-tree-zigzag-level-order-traversal",
        round: 2,
        ans: "stacktree",
        note: "Same BFS-by-level template as in the library answer; flip the insertion end per level with a deque instead of reversing the list.",
      },
      {
        q: "Design a scheduler (cron-like task scheduling service)",
        t: "lld",
        lv: ["SDE2", "Lead SDE", "Staff SDE"],
        d: "H",
        round: 3,
        ans: "lld-roster",
        note: "The on-call roster answer transfers directly: deterministic schedule generation, overrides, a timing wheel or delay queue for firing, and idempotent execution with leases so a crashed worker's task re-runs exactly once.",
      },
      {
        q: "Design a message queue",
        t: "hld",
        lv: ["Lead SDE", "Staff SDE"],
        d: "H",
        round: 4,
        ans: "hld-logging",
        note: "Borrow the Kafka half of the logging answer: partitions for ordering and parallelism, offsets and consumer groups, at-least-once plus idempotent consumers, retention and replay, and backpressure.",
      },
      {
        q: "Design a Twitter-like feed",
        t: "hld",
        lv: ["SDE2", "Lead SDE", "Staff SDE"],
        d: "H",
        round: 4,
        ans: "hld-social",
      },
      {
        q: "Design a URL shortener",
        t: "hld",
        lv: ["SDE", "SDE2", "Lead SDE"],
        d: "M",
        round: 4,
        a: "**Clarify**: vanity URLs? expiry? analytics? read:write ratio (heavily read-skewed, ~100:1)? Scale — say 100M new links/month, 10K redirects/sec peak.\n\n**Key generation** is the whole question. Three options: (1) hash the long URL (MD5/SHA, base62 the first 43 bits) and handle collisions with a retry salt — deterministic, dedupes repeats; (2) a distributed counter (Snowflake or a range-allocating ticket server) base62-encoded — no collisions, but sequential ids are guessable and enumerable; (3) pre-generate a key pool in a database and hand out unused keys — simple and collision-free, at the cost of a table to maintain. Say which and why: counter + base62 with a random offset is the usual production answer.\n\n**Storage** — a single key–value table `(short_key PK, long_url, owner, created_at, expires_at)`. Billions of rows, one access pattern (key → URL), no joins: that is a key–value store (DynamoDB/Cassandra) or a sharded SQL table keyed by the short key. **Redirect path**: check Redis, fall back to the store, then `301` (cacheable, loses analytics) or `302` (keeps analytics). **Analytics** goes async through Kafka, never inline.\n\n**Scale**: 7 chars of base62 gives 3.5 trillion keys. 100M links/month × 500 bytes ≈ 50 GB/month, trivial. All the difficulty is read latency, so cache the hot 20% and serve from the edge.",
        fu: [
          {
            q: "How do you prevent two servers generating the same key?",
            a: "A single source of monotonic ids (ticket server, Snowflake with machine id, or Redis INCR ranges handed out in blocks of 10,000 so the coordinator is hit rarely). Hash-based keys need a uniqueness check on write — a conditional insert that fails on duplicate.",
          },
          {
            q: "301 or 302?",
            a: "302 (temporary) if you need per-click analytics or may change the target; 301 if you want browsers and CDNs to cache and cut your traffic. Most shorteners use 302 for exactly the analytics reason.",
          },
          {
            q: "How do you expire billions of links?",
            a: "TTL in the key–value store (DynamoDB TTL, Cassandra TTL) or a partitioned-by-month table you can drop. Never a DELETE sweep over billions of rows.",
          },
        ],
      },
      {
        q: "HashMap internals: how does it work, and what changed in Java 8?",
        t: "concept",
        lv: ["SDE", "SDE2", "Lead SDE"],
        round: 2,
        a: 'Array of buckets indexed by `(n-1) & hash`, with `hash` being the key\'s hashCode spread by XORing its high bits down (`h ^ (h >>> 16)`) so poor hash functions still distribute. Collisions chain in a linked list; since **Java 8**, a bucket with more than 8 entries (and a table of at least 64) converts to a red-black tree, so worst-case lookup goes from O(n) to O(log n) — the fix for hash-collision DoS.\n\nResize doubles capacity at `size > capacity × loadFactor` (0.75) and rehashes; in Java 8 entries split into a "lo" and "hi" list without recomputing hashes. Key contract: equal objects must have equal hashCodes, and mutating a key after insertion loses it.\n\nConcurrency: `HashMap` is not thread-safe (pre-Java 8 it could even loop forever on concurrent resize); use `ConcurrentHashMap`, which locks per bin with CAS on the first insert and uses `synchronized` on the bin head thereafter.',
      },
      {
        q: "Deadlock: how do you detect and prevent it?",
        t: "concept",
        lv: ["SDE2", "Lead SDE", "Staff SDE"],
        round: 3,
        ans: "c-locks",
      },
      {
        q: "SOLID principles — with an example from your code",
        t: "concept",
        lv: ["SDE", "SDE2", "Lead SDE"],
        round: 3,
        ans: "c-patterns",
        note: "Microsoft asks this as a fundamentals check. Answer with one real refactor you did, not the textbook definitions.",
      },
    ],
    prep: [
      "The LLD round wants running code, not boxes — practise writing a cache, a scheduler and a key-value store with TTL in 45 minutes.",
      "Threading questions appear in almost every SDE II+ loop: locks, deadlock, ConcurrentHashMap, producer/consumer.",
      "The 'as appropriate' round is senior and unpredictable; have your project deep-dive and your strongest design ready.",
      "Fundamentals (OS, DBMS, networking) still come up at Microsoft more than at most product companies.",
    ],
    sources: [
      {
        label: "LeetCode — Microsoft SDE-2 recent questions 2025 (consolidated)",
        url: "https://leetcode.com/discuss/interview-question/6403987/Microsoft-SDE-2-Recent-questions-2025-or-Consolidated/",
      },
      {
        label: "LeetCode — Microsoft SDE 2 (L61) Hyderabad experience",
        url: "https://leetcode.com/discuss/post/7541629/interview-experience-microsoft-sde-2l61-qvanx/",
      },
      {
        label: "GeeksforGeeks — Microsoft SDE-II experience",
        url: "https://www.geeksforgeeks.org/interview-experiences/microsoft-interview-experience-for-sde-ii/",
      },
    ],
  },
  {
    id: "meta",
    name: "Meta",
    sector: "product",
    tag: "Timed coding (two problems in 35 min), then design that decides your level.",
    loc: "Bengaluru · Hyderabad",
    ladder: [
      {
        lvl: "SDE",
        internal: "E3",
        yoe: "0–2",
        loop: "Screen + 3 onsite (2 coding, 1 behavioural)",
        bar: "Speed and correctness on Medium problems.",
      },
      {
        lvl: "SDE2",
        internal: "E4",
        yoe: "2–5",
        loop: "Screen + 4 onsite (2 coding, 1 design, 1 behavioural)",
        bar: "Two Mediums in 35–40 min; design is lighter weight.",
      },
      {
        lvl: "Lead SDE",
        internal: "E5 (Senior)",
        yoe: "5–9",
        loop: "Screen + 4 onsite (2 coding, 1 system design or product architecture, 1 behavioural)",
        bar: "Design and behavioural carry the most weight and decide E5 vs down-level to E4; ownership of projects needing 3+ people.",
      },
      {
        lvl: "Staff SDE",
        internal: "E6 (Staff)",
        yoe: "9–13",
        loop: "2 design rounds, coding, leadership/behavioural",
        bar: "Org-level impact, ambiguous problem framing, technical direction across teams.",
      },
      {
        lvl: "Principal SDE",
        internal: "E7+",
        yoe: "13+",
        loop: "Design + leadership panel with directors",
        bar: "Company-level bets, multi-org influence.",
      },
    ],
    loop: [
      {
        n: 1,
        name: "Online assessment (CodeSignal)",
        dur: "90 min",
        fmt: "Proctored",
        tests:
          "One complex problem in four escalating stages — most candidates do not finish all four.",
        levels: ["SDE", "SDE2"],
      },
      {
        n: 2,
        name: "Technical phone screen",
        dur: "45 min",
        fmt: "CoderPad",
        tests:
          "~35 minutes of actual coding: two Medium problems. You must dry-run mentally — no execution.",
        levels: ["SDE", "SDE2", "Lead SDE", "Staff SDE"],
      },
      {
        n: 3,
        name: "Coding × 2",
        dur: "45 min each",
        fmt: "CoderPad",
        tests:
          "1–2 problems per round, Medium to Hard, heavily from the Meta-tagged set. Minor syntax bugs are forgiven; wrong approach is not.",
        levels: ["SDE", "SDE2", "Lead SDE", "Staff SDE"],
      },
      {
        n: 4,
        name: "System design or product architecture",
        dur: "45 min",
        fmt: "Excalidraw",
        tests:
          "Product architecture = user-facing (Instagram, ticketing); system design = infra (caches, rate limiters, pipelines). E5+ only.",
        levels: ["Lead SDE", "Staff SDE", "Principal SDE"],
      },
      {
        n: 5,
        name: "Behavioural (loop lead)",
        dur: "45 min",
        fmt: "Conversation",
        tests:
          "Conflict, driving results, ambiguity, growth, communication — two stories per competency with quantified impact.",
        levels: ["SDE", "SDE2", "Lead SDE", "Staff SDE", "Principal SDE"],
      },
    ],
    bank: [
      {
        q: "Minimum Remove to Make Valid Parentheses",
        t: "coding",
        lv: ["SDE", "SDE2", "Lead SDE"],
        d: "M",
        lc: 1249,
        slug: "minimum-remove-to-make-valid-parentheses",
        round: 3,
        a: "The single most-asked Meta question. **One pass with a stack of indices**: push `(` indices, pop on `)`; an unmatched `)` marks itself for removal, and whatever remains on the stack at the end marks unmatched `(`. Build the result skipping marked indices. O(n) time, O(n) space.\n\n**Two-pass O(1)-extra alternative** (worth offering): count open/close left-to-right dropping surplus `)`, then right-to-left dropping surplus `(`.\n\nWatch the detail they grade: letters pass through untouched, and the answer must be *any* valid minimum removal — you do not need a specific one.",
        code: {
          java: "String minRemoveToMakeValid(String s) {\n    char[] arr = s.toCharArray();\n    Deque<Integer> open = new ArrayDeque<>();\n    for (int i = 0; i < arr.length; i++) {\n        if (arr[i] == '(') open.push(i);\n        else if (arr[i] == ')') {\n            if (open.isEmpty()) arr[i] = '#';     // unmatched ')'\n            else open.pop();\n        }\n    }\n    while (!open.isEmpty()) arr[open.pop()] = '#'; // unmatched '('\n\n    StringBuilder sb = new StringBuilder();\n    for (char c : arr) if (c != '#') sb.append(c);\n    return sb.toString();\n}",
          py: "def min_remove_to_make_valid(s):\n    arr = list(s)\n    stack = []\n    for i, c in enumerate(arr):\n        if c == '(':\n            stack.append(i)\n        elif c == ')':\n            if stack:\n                stack.pop()\n            else:\n                arr[i] = ''\n    for i in stack:\n        arr[i] = ''\n    return ''.join(arr)",
        },
      },
      {
        q: "Binary Tree Vertical Order Traversal",
        t: "coding",
        lv: ["SDE2", "Lead SDE"],
        d: "M",
        lc: 314,
        slug: "binary-tree-vertical-order-traversal",
        round: 3,
        ans: "treeviews",
        note: "Same BFS-with-horizontal-distance technique as the top view answer; collect into a map keyed by column instead of keeping only the first node.",
      },
      {
        q: "Basic Calculator II",
        t: "coding",
        lv: ["SDE2", "Lead SDE"],
        d: "M",
        lc: 227,
        slug: "basic-calculator-ii",
        round: 3,
        a: "Single pass with a stack and a `lastOp` variable. Accumulate digits; when you hit an operator or the end, apply the *previous* operator: `+` pushes the number, `-` pushes its negation, `*` and `/` pop and combine immediately (that is how precedence is handled without a parser). The answer is the sum of the stack. O(n) time, O(n) space.\n\nThe O(1)-space version keeps `result`, `prev` and applies the same logic without a stack — offer it as the follow-up.\n\nEdge cases they check: spaces anywhere, multi-digit numbers, integer division truncating toward zero, and a trailing number (hence the `i == n-1` condition in the loop).",
        code: {
          java: "int calculate(String s) {\n    Deque<Integer> stack = new ArrayDeque<>();\n    int num = 0; char lastOp = '+';\n    for (int i = 0; i < s.length(); i++) {\n        char c = s.charAt(i);\n        if (Character.isDigit(c)) num = num * 10 + (c - '0');\n        if ((!Character.isDigit(c) && c != ' ') || i == s.length() - 1) {\n            switch (lastOp) {\n                case '+' -> stack.push(num);\n                case '-' -> stack.push(-num);\n                case '*' -> stack.push(stack.pop() * num);\n                case '/' -> stack.push(stack.pop() / num);\n            }\n            lastOp = c; num = 0;\n        }\n    }\n    int result = 0;\n    while (!stack.isEmpty()) result += stack.pop();\n    return result;\n}",
          py: "def calculate(s):\n    stack, num, last_op = [], 0, '+'\n    for i, c in enumerate(s):\n        if c.isdigit():\n            num = num * 10 + int(c)\n        if (not c.isdigit() and c != ' ') or i == len(s) - 1:\n            if last_op == '+':   stack.append(num)\n            elif last_op == '-': stack.append(-num)\n            elif last_op == '*': stack.append(stack.pop() * num)\n            else:                stack.append(int(stack.pop() / num))  # trunc toward 0\n            last_op, num = c, 0\n    return sum(stack)",
        },
      },
      {
        q: "Kth Largest Element in an Array",
        t: "coding",
        lv: ["SDE", "SDE2"],
        d: "M",
        lc: 215,
        slug: "kth-largest-element-in-an-array",
        round: 2,
        ans: "kthstream",
        note: "Meta asks the array form: min-heap of size k in O(n log k), or Quickselect in O(n) average — mention Quickselect's O(n²) worst case and the median-of-medians fix.",
      },
      {
        q: "Valid Word Abbreviation",
        t: "coding",
        lv: ["SDE", "SDE2"],
        d: "E",
        lc: 408,
        slug: "valid-word-abbreviation",
        round: 2,
        a: 'Two pointers over word and abbreviation. On a digit, reject a leading zero, parse the full number, and jump the word pointer by it; otherwise the characters must match. Valid only if both pointers finish together. O(n) time, O(1) space.\n\nThe traps are all in the parsing: `"01"` is invalid, the number can be multi-digit, and the jump can overflow past the end of the word — check bounds before comparing.',
        code: {
          java: "boolean validWordAbbreviation(String word, String abbr) {\n    int i = 0, j = 0;\n    while (i < word.length() && j < abbr.length()) {\n        if (Character.isDigit(abbr.charAt(j))) {\n            if (abbr.charAt(j) == '0') return false;      // no leading zeros\n            int num = 0;\n            while (j < abbr.length() && Character.isDigit(abbr.charAt(j)))\n                num = num * 10 + (abbr.charAt(j++) - '0');\n            i += num;\n        } else {\n            if (word.charAt(i++) != abbr.charAt(j++)) return false;\n        }\n    }\n    return i == word.length() && j == abbr.length();\n}",
          py: "def valid_word_abbreviation(word, abbr):\n    i = j = 0\n    while i < len(word) and j < len(abbr):\n        if abbr[j].isdigit():\n            if abbr[j] == '0':\n                return False\n            num = 0\n            while j < len(abbr) and abbr[j].isdigit():\n                num = num * 10 + int(abbr[j]); j += 1\n            i += num\n        else:\n            if word[i] != abbr[j]:\n                return False\n            i += 1; j += 1\n    return i == len(word) and j == len(abbr)",
        },
      },
      {
        q: "Design an ad click aggregator (product architecture)",
        t: "hld",
        lv: ["Lead SDE", "Staff SDE"],
        d: "H",
        round: 4,
        a: "**Clarify**: real-time dashboard latency (seconds vs minutes), retention, exactly-once vs approximate counting, and the query shape (clicks per ad per minute/hour/day, with filters).\n\n**Write path** — click events (~1M/s at Meta scale) go to a durable log (Kafka) partitioned by ad id. A stream processor (Flink) does windowed aggregation per (ad, minute) with event-time windows and watermarks for late events, writing to an OLAP store (Druid/ClickHouse/Cassandra) keyed by `(ad_id, minute)`. Roll minutes up to hours and days as continuous aggregates — the same pre-aggregation argument as the moving-average design in the library.\n\n**Read path** — dashboards query the coarsest granularity that answers the question; recent minutes may come from the stream processor's state for freshness.\n\n**The hard parts they probe**: (1) idempotency — deduplicate by click id in a windowed set so retries do not double count; (2) hot ads — salt the partition key and merge on read; (3) late/out-of-order events — allowed lateness plus a correction path; (4) exactly-once — Flink checkpoints plus an idempotent sink, and be honest that end-to-end it is effectively at-least-once with dedup.\n\n**Storage**: 1M clicks/s × 100 bytes ≈ 100 MB/s raw. Raw goes to S3 for reprocessing; only aggregates stay hot.",
        fu: [
          {
            q: "Ad-fraud filtering?",
            a: "A second consumer of the same topic scoring clicks, with fraudulent ones written to a correction stream that subtracts from aggregates — never mutate the original event.",
          },
          {
            q: "How do advertisers see 'real-time' numbers that later change?",
            a: "Show two tiers: a fast approximate counter (HyperLogLog/Redis) for the live view, and the batch-corrected number for billing, clearly labelled. Billing must be the batch number.",
          },
        ],
      },
      {
        q: "Design Instagram / a photo-sharing feed (product architecture)",
        t: "hld",
        lv: ["Lead SDE", "Staff SDE"],
        d: "H",
        round: 4,
        ans: "hld-instagram",
      },
      {
        q: "Design a top-K / trending system (Spotify top-K songs widget)",
        t: "hld",
        lv: ["Lead SDE", "Staff SDE"],
        d: "H",
        round: 4,
        a: "**Clarify**: top-K over what window (last hour, day, all time), how many distinct items, exact or approximate, and how fresh.\n\n**Exact, small scale** — a count in Redis per item plus a sorted set (ZSET) of scores; `ZINCRBY` on each play and `ZREVRANGE 0 K` to read. Fine to millions of items.\n\n**Approximate, huge scale** — **Count-Min Sketch** for frequency estimates in fixed memory plus a heap of the current top K; per-node sketches merged centrally. State the trade: sublinear memory, bounded overestimation, no exact answer.\n\n**Windowed** — bucket counts per minute and keep a ring of buckets; the window total is the sum of the last N buckets, so expiry is dropping a bucket rather than decaying every key. This is the design detail that separates a good answer here.\n\n**Serving** — the widget reads a precomputed top-K list from cache (refreshed every few seconds); never compute top-K per request. Add per-region lists since trending is local.",
        fu: [
          {
            q: "Why not just sort the counts?",
            a: "Sorting billions of counters per query is O(n log n) per request. The heap keeps only K, and the sketch keeps counts in fixed memory — the point is to never materialise the full distribution.",
          },
          {
            q: "How do you prevent one client spamming the top list?",
            a: "Rate limit per user and count distinct users (HyperLogLog) rather than raw events — trending should measure reach, not volume.",
          },
        ],
      },
      {
        q: "Tell me about a conflict with a coworker and how it resolved",
        t: "behavioral",
        lv: ["SDE2", "Lead SDE", "Staff SDE", "Principal SDE"],
        round: 5,
        a: "Meta's behavioural round is run by the loop lead and is one of the two rounds that decide E5 vs E4, so treat it as technical-weight. The competencies are conflict resolution, driving results, ambiguity, growth, and communication — prepare **two stories each** with numbers.\n\nFor conflict: pick a real technical disagreement with a peer (not a manager), show that you sought their reasoning first, brought data, and describe the resolution mechanism (a spike, a prototype, a written comparison). Say what the relationship looked like afterwards — Meta is explicitly checking whether you can keep moving fast with people you disagree with.\n\nAvoid conflicts resolved by escalation alone, and avoid stories where you were simply right.",
      },
      {
        q: "Describe a project you drove that needed three or more people",
        t: "behavioral",
        lv: ["Lead SDE", "Staff SDE"],
        round: 5,
        a: 'This is the E5 bar written as a question: ownership of something that required coordinating others. Structure: the ambiguous problem, how you scoped it into shippable pieces, how you divided work and kept it coherent (design doc, interfaces, weekly checkpoint), what went wrong mid-way, and the quantified result.\n\nName your own contribution precisely — Meta interviewers are trained to ask "what did *you* do" twice. Include one decision you made that a less senior engineer would not have (cutting scope, changing the data model, deferring a migration) and its trade-off.',
      },
    ],
    prep: [
      "Drill the Meta-tagged LeetCode set for speed: two Mediums in 35 minutes with no compiler is the actual constraint.",
      "Practise dry-running code aloud — CoderPad does not execute, and verification is a scored dimension.",
      "For E5+, the design and behavioural rounds decide your level; prepare both with quantified project stories.",
      "Know the difference between the product architecture and system design variants and ask which one you are in.",
    ],
    sources: [
      {
        label: "Hello Interview — Meta E5 guide",
        url: "https://www.hellointerview.com/guides/meta/e5",
      },
      {
        label: "Hello Interview — Meta E6 guide",
        url: "https://www.hellointerview.com/guides/meta/e6",
      },
      {
        label: "Prepfully — Meta software engineer guide",
        url: "https://prepfully.com/interview-guides/meta-software-engineer",
      },
    ],
  },
];
